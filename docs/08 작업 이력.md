---
tags: [history, changelog]
updated: 2026-08-10
---

# 08 작업 이력

> 모든 작업은 2026-07-04 하루 동안 Claude Code 세션에서 진행됨.
> 이후 작업은 이 문서에 날짜별로 이어서 기록할 것.

## 2026-07-04

### 1. 초기 구축
- 요구사항: 다수 LLM이 동일 레퍼런스/키워드로 동시 조사 → 상호 토론 →
  우수 내용 선별/종합 → PPT·Word·Excel 보고서 생성
- 결정: **멀티 프로바이더**(Claude+GPT+Gemini, 사용자 선택) + **Streamlit UI**
- 구현: config / providers 3종 / pipeline(조사·토론·종합) / reports 3종 / app.py
- 검증: 보고서 빌더 스모크 테스트, UI 렌더링 확인

### 2. Gemini 키 등록 + 버그 수정
- `GOOGLE_API_KEY` 등록, 기본·웹검색 그라운딩 검증
- **버그**: 짧은 max_tokens에서 빈 응답 → thinking 토큰 차감이 원인
  → 출력 하한 8192 + `_extract_text()` 방어 로직 (→ [[03 LLM 프로바이더]])
- Gemini 단독 e2e (조사→종합→3종 파일) 성공

### 3. 레퍼런스 자동 탐색 기능
- `core/discovery.py` — LLM 웹 검색으로 신뢰 출처 발굴, 체크박스 선택 후
  조사에 병합. `parse_json_loose` 배열 지원 확장
- 실검증: EU CBAM 주제 → EU 집행위·산업부·KIEP·KITA 등 발굴

### 4. Anthropic 키 등록 + 폴백 보강
- 키 유효 확인, 단 **크레딧 잔액 0** (`credit balance too low` 400)
- `discover_with_fallback()` 추가 — 탐색 실패 시 다음 프로바이더로 자동 전환
- 실검증: Claude 실패 → Gemini 폴백, 조사 단계 오류 격리, 진행자 자동 재선택

### 5. 보고서 분량 설정
- 사이드바 슬라이더 (처음 8~20장 → 이후 **1~20장** 확장)
- `_length_plan()` — 장수 역산 (원페이저/컴팩트/표준 3계층)
- PPT 빌더에 원페이저·컴팩트 레이아웃 추가
- 검증: 1~20 전 구간 정합 + 실행 테스트 (10장→10장, 1장→원페이저 1장)

### 6. 파일 첨부 레퍼런스
- `core/filerefs.py` — pdf/docx/pptx/xlsx/txt/md/csv 본문 추출 (pypdf 추가)
- 형식별 샘플 생성 테스트 전부 통과 (cp949 CSV, 한글 표 포함)

### 7. 외부 PPT 디자인 엔진 (키 대기 스캐폴드)
- 사전 조사: Gamma(공식 API+MCP)·Canva(공식 MCP+Connect API)·
  Google Slides(공식 API)는 연동 가능 / Beautiful.ai·Genspark·Skywork는
  공개 API 없어 보류 (→ [[06 PPT 디자인 엔진]])
- `core/ppt_engines/` — PPTEngine 추상화 + 3개 엔진 구현
  - Gamma: Generate API (마크다운 카드 → 디자인 덱 + PPTX)
  - Canva: Design Import API (로컬 PPTX → 편집 가능한 Canva 디자인)
  - Google Slides: 서비스 계정 + batchUpdate (공유 링크)
- UI: 다운로드 탭에 엔진 선택·실행·결과 링크, 키 없으면 발급 안내
- 검증: 레지스트리/가용성/마크다운 변환 테스트 (실API는 키 등록 후)

### 8. 설계서 볼트 생성 (이 문서)
- `docs/` 폴더를 옵시디언 볼트로 사용

### 9. 회사 홈페이지에 제품 등록 + 배포 준비
- 요구사항: anfilt-homepage.netlify.app 에 "ANFILT Product" 메뉴 + "Research Agent"
  제품 등록, 홈페이지에서 실행 가능하게
- 기술 제약 확인: Streamlit은 Netlify에서 못 돌림 → 앱(Streamlit Cloud) + 홈페이지 분리
- 접근 정책 결정(사용자): **내부용** (Invite only, 공개 실행 URL 미노출)
- 앱 안전화: `.gitignore`(키 유출 차단), `app.py` secrets 브리지, `.streamlit/*`, `DEPLOY.md`
- 홈페이지: `concept-c.html`(=`dist/index.html`)에 `#product` 섹션 + 내비 링크 추가,
  `APP_URL` 한 줄로 실행 링크 제어 (기존 CONFIG 폴백 철학 준수)
- GitHub 비공개 repo `0204yong/anfilt-research-agent` 푸시 (키 gitignore 검증 후),
  Iron & Ink 디자인 일치 렌더 확인
- 검증 워크플로(4관점 적대적 교차검증)로 배포 전 문서/보안/HTML/배포 정확성 점검·수정
- 상세: [[10 배포와 홈페이지 연동]]

### 10. 실제 배포 완료 (2026-07-04)
- **홈페이지**: 사용자가 `dist/` 드래그앤드롭 → Netlify 라이브에 제품 섹션 반영(curl 검증).
- **앱**: `claude-in-chrome` 확장으로 브라우저 자동조작해 Streamlit Community Cloud 배포.
  - 앱 URL: `https://anfilt-research-agent-qgb4tpo5e2dvckjh6pmpmi.streamlit.app/`
  - Python 3.14.6 빌드 성공, Claude·Gemini 키 인식 확인(secrets 브리지 정상).
- **이슈 해결**: Streamlit이 비공개 repo 못 찾음 → repo 공개 전환. `.env`가 AnySign4PC와
  연결돼 암호 요구 → 메모장으로 열기 안내.
- **에이전트 제약**: API 키 입력·접근권한(Invite only) 변경·repo 공개전환은 규정상 불가 →
  해당 단계는 사용자가 직접 수행.
- **Invite only 잠금 완료**: 사용자가 Settings→Sharing에서 "Only specific people can view"
  + `0204yongko@gmail.com` 설정·저장. 모달 재오픈으로 저장 유지 확인 → 내부용 완성.
  (팀원 추가 시 이메일을 콤마로 더 넣고 Save)
- 상세: [[10 배포와 홈페이지 연동]]

### 11. 원커맨드 자동 배포 체계 구축 (2026-07-04)
- 요구(사용자): "일부 수정 후 배포 명령만 하면 내 개입 없이 전부 배포되게 하라"
- 홈페이지를 git화(`0204yong/anfilt-homepage` 비공개) + `netlify.toml`(빌드: concept-c.html→dist)
  → Netlify 사이트에 저장소 연결(사용자: GitHub OAuth·앱설치 승인 / 에이전트: 나머지 위저드)
- **이슈**: 무료 플랜 비공개 repo는 "미인식 기여자" push의 빌드 차단 →
  Netlify 팀 설정 Git Contributors에서 GitHub(0204yong) 연결로 해결 (repo 비공개 유지)
- **이슈**: 시크릿 검사 패턴이 SKILL.md 속 패턴 문자열에 오탐 → 실키 형태 정규식으로 정밀화
- e2e 증명: HTML 마커 커밋 push→라이브 자동 반영 확인, 마커 제거 push로 2차 확인
- 산출물: `/deploy` 스킬(.claude/skills/deploy), `C:\ANFILT_AI\deploy.ps1`, DEPLOY.md 개편
- 상세: [[11 원커맨드 배포]]

### 12. 진행자 채점표 기능 (2026-07-04)
- 요구(사용자): "베스트 선정 기준을 채점표로 먼저 출력하고 그걸 근거로 선정,
  왜 베스트였는지 보고서와 함께 확인 + 채점 항목 추가"
- 파이프라인이 조사→토론→**채점**→종합 4단계로 확장: `run_scoring()` 신설
  - 진행자 LLM이 연구원별 최종 입장을 기준당 1~10점으로 채점 후 best 선정
  - 채점표가 종합 프롬프트에 "사전 채점 결과" 블록으로 주입돼 선정 근거가 됨
- 채점 기준 9종(`SCORING_CRITERIA`), 기본 6종: 정확성/근거·출처/완결성/최신성/
  정량성/논리 일관성 + 선택 3종: 실행가능성/균형·객관성/토론 수용성 (UI multiselect)
- 안전장치: 점수 1~10 코드 클램프 + 총점 코드 재계산(LLM 산술 불신),
  채점 실패 시 채점표 없이 종합 계속, 성공 연구원 2 미만이면 생략
- UI: 결과 화면에 **🏅 채점표 탭** — 베스트+선정이유, 기준×연구원 점수 매트릭스,
  기준별 채점 사유·강점·약점. 익명 이름에 실제 LLM 병기 ("연구원 A (Gemini)")
- 검증: Gemini 실호출 채점 테스트(품질 우수 58 vs 부실 13, best 정확 선정),
  AppTest UI 렌더링 테스트 통과. 상세: [[02 파이프라인 설계]]

### 13. 홈페이지 제품 페이지 분리 + 상담 관리자 화면 + 이메일 제거 (2026-07-04)
- 요구(사용자): ① 제품 소개를 랜딩에서 빼고 상단 메뉴로만 들어가는 별도 페이지로
  ② 무료상담신청 관리자 화면(누가·언제·무슨 내용 + 회신 관리) ③ 화면의 직접 연락 이메일 삭제
- 구조: concept-c.html(랜딩 /) · product.html(/product/) · admin.html(/admin/),
  공용 CSS를 assets/site.css로 추출(3페이지 공유). netlify.toml 빌드 3페이지로 확장
- 관리자 화면: Supabase Auth 로그인 → leads 목록(신청일시·회사·담당자·연락처·내용) +
  회신완료 토글(replied/replied_at)·메모(admin_note). 필터/건수. 모든 신청자 입력 esc()
- DB: supabase-admin-setup.sql — leads에 회신 컬럼 추가 + **관리자 UID 기반 RLS**
  (SELECT/UPDATE) + 익명 INSERT 강화(회신상태 위조 차단)
- 이메일: 상담 직접연락·푸터 mailto·실패 안내문·CONFIG.CONTACT_EMAIL 전부 제거
- **적대적 검증 워크플로**(5관점 리뷰→교차검증): 세션 한도로 교차검증은 중단됐으나
  완료된 3개 리뷰의 발견을 직접 검증·수정 —
  (1) site.css 헤더 한글 mojibake(PowerShell heredoc 인코딩) → Edit로 정상화
  (2) 세션 만료 시 개인정보 잔존 → onAuthStateChange로 즉시 화면 잠금
  (3) [major] JWT email만으로 관리자 판별 → **UID 기반 정책으로 교체**(이메일 위조 무력화)
- 검증: 로컬 dist 빌드→프리뷰로 3페이지 렌더·콘솔무오류·로그인 실패 흐름·이메일 0건 확인
- 상세: [[12 홈페이지 제품 페이지와 상담 관리]]

### 14. 공개 배포용 비밀번호 게이트 (2026-07-05)
- 요구(사용자): Research Agent 앱을 "누구나 접속 가능한 링크"로 공개
- 리스크 고지: 앱은 소유자 API 키로 작동 → 완전 공개 시 아무나 소유자 비용으로 사용
  (AskUserQuestion) → 사용자 선택: **비밀번호 잠금 후 공개**
- 구현: `app.py`에 `_require_password()` — APP_PASSWORD(env/Secrets) 설정 시 진입 시
  비밀번호 요구(세션당 1회, `hmac.compare_digest` UTF-8), **미설정 시 fail-closed**(앱 안 열림).
  `st.set_page_config` 직후 호출, 미인증이면 `st.stop()`으로 본문 차단. secrets/env 예시에 추가
- 검증: 프리뷰로 3경로(미설정→잠금 / 틀린비번→거부 / 맞는비번→입장) 실측 통과
- **적대적 보안 감사 워크플로**(우회/fail-open/유출/Streamlit 4관점, Opus 4.8): **0건**
- 공개 순서(사용자): ①Streamlit Secrets에 APP_PASSWORD 저장 → ②Sharing "Anyone with the link"
  → 접근권한 변경은 규정상 사용자가 수행. 상세: [[07 설치와 실행]]

## 2026-07-06

### 15. 지식 볼트·온톨로지 설계 (설계만, 구현 미착수)
- 요구(사용자): 조사 결과물이 사라지지 않게 저장·재열람 + 사용자만의 온톨로지를
  Obsidian으로 축적 + 유사 주제 재조사 시 축적 지식 위에서 더 깊은 조사
- 설계 방법: 멀티에이전트 워크플로 — 코드 분석 4건 → 독립 설계안 3건(파일 우선/
  Supabase 우선/지식그래프 우선) → 3관점 심사(사용자 적합성/엔지니어링/지식 품질)
- 확정안: **이중 구조** — 1층 실행 아카이브(Supabase `ra_runs` jsonb, 재열람·필터),
  2층 지식 볼트(Obsidian 마크다운이 원본, 노트=엔티티, 타입 6종·술어 12종 enum,
  시드 30노트). 주입은 `reference_texts["[축적 지식] ..."]` 채널로 pipeline 무수정
- 핵심 안전장치(심사 발견): ① 주입 라벨 "미검증·대조 대상"(환각 세탁 방지)
  ② 채점 단계 주입 금지(신규 발견 저평가 방지) ③ 고객사 기밀 격리(confidential
  플래그 — NDA 자료의 타 고객사 유출 차단) ④ 저장 실패 시 zip 폴백(결과 증발 방지)
- 코드상 주의: `PipelineResult`에 brief·실행 파라미터 미포함 → 훅은 app.py:328
  부근에서 함께 캡처. 상세 설계·로드맵 5단계: [[13 지식 볼트와 온톨로지]]
- 다음 세션: 로드맵 1단계(볼트 zip 내보내기)부터 구현 예정

## 2026-07-11

### 16. 라이트 모드 방향 확정 (설계 논의)
- 배경(사용자): 연동된 LLM API 비용이 부담된다는 사용자 의견 +
  `ESG_에이전트_업무패턴_분석.docx`(사용자 업무 패턴 5가지) 접수
- 방향 확정: **지식창고 우선(Knowledge-first) 라이트 모드** — 0단계 볼트/아카이브
  무료 검색 → 1단계 저가 모델 1회 호출(토론·채점 생략) → 2단계 볼트 업서트.
  업무 템플릿 선택형·클라이언트 컨텍스트 분리·온디맨드 교차검증 포함
- 구조 결정: 같은 저장소, `app.py` 모드 토글(라이트/풀). **지식볼트는 라이트·풀
  공용 기반** → 볼트(문서 13 로드맵)를 먼저 구현하기로 확정

### 17. 지식볼트 1단계 — zip 내보내기 구현 ✅
- `core/vault_render.py` 신규 (Streamlit 비의존): 결과 → Obsidian 노트(.md) +
  run.json(schema_version=1, 2단계 Supabase 적재 호환) → zip 바이트
  - 노트: frontmatter(run_id/as_of/providers/verified:false) + 미검증 경고 콜아웃 +
    보고서·채점표(익명↔실명 병기)·개별 조사·토론 전문. 표 파이프 이스케이프·행 패딩
  - run.json: brief+params+result 전체 + 레퍼런스 프로비넌스(content_hash·
    is_error `'['` 관례·is_attachment — 기밀 격리 3단계 대비)
  - 파일명 정규화: Windows 금지문자·예약명(CON 등)·꼬리 점, zip은 UTF-8 플래그 자동
- `app.py`: 실행 성공 시 `run_ctx`(brief+파라미터+executed_at) 캡처(설계서가 지적한
  PipelineResult 누락 보완), 다운로드 탭에 "🗂 지식볼트 zip 다운로드" 추가
- 검증: 더미 파이프라인 결과 스모크 테스트 전 항목 통과(zip 구조·frontmatter YAML
  파싱·이스케이프·프로비넌스·예약명), `py_compile` 통과

### 18. 지식볼트 2단계 — Supabase 아카이브+재열람 구현 ✅ (활성화는 사용자 설정 대기)
- `supabase-ra-runs-setup.sql` — `ra_runs` 테이블(run_id PK, jsonb record,
  schema_version) + RLS 전면 차단(anon/authenticated) — 접근은 service_role만
- `core/store.py` (Streamlit 비의존) — PostgREST REST 직접 호출(신규 의존성 0):
  save_run(업서트)/list_runs/load_run/record_to_state(필드 명시 선별 복원)
- `app.py` — 성공 직후 자동 저장(`_saved_run_id` 중복 방지), 실패 시 경고+재시도+
  zip 폴백 안내(안전장치 4), 사이드바 "🗂 이전 조사"(60초 캐시)→5탭 UI 복원,
  run_id를 zip·아카이브가 공유, `.env.example`에 SUPABASE 2항목 추가
- 검증: 왕복 복원·REST 모킹(업서트 헤더·오류 노출·빈 결과)·여분 키 내성·
  run_id 공유 테스트 전부 통과. UI 부팅 검증 — 키 미설정 시 안내 캡션만, 예외 없음
- **라이브 검증 완료 (같은 날)**: 사용자가 SQL 실행(처음에 파일명만 붙여넣는
  실수 → 본문 붙여넣기로 해결) + .env에 키 입력(에이전트가 빈 항목 추가 후
  메모장 열어 사용자가 키만 기입 — 키는 채팅에 노출 안 함).
  CLI 왕복(저장→중복 없음→로드→복원→삭제) + 앱 e2e(실조사→자동 저장→새 세션
  불러오기→5탭 복원→볼트 zip) 전부 실측 통과
- 버그 픽스: 사이드바(상단 렌더)가 저장(하단 실행) 결과를 못 보던 문제 →
  저장 성공 직후 `st.rerun()` + 토스트 대신 지속 캡션("🗂 이력 저장됨")
- 배포: 커밋 `edccf4d` push → Streamlit 재배포, 사용자가 Secrets에 SUPABASE
  2줄 + APP_PASSWORD 입력. **앱 URL 변경**(사용자, 같은 날):
  `anfilt-research-agent-qgb4tpo5e2dvckjh6pmpmi` → **`anfilt-research-agent`**.streamlit.app
  (옛 무작위 주소 접속 불가, deploy 스킬·문서 10 갱신함). 새 URL에서 새 코드
  부팅·비밀번호 게이트 표시 확인(브라우저 실측), 익명 303 = Invite only 유지

### 19. 지식볼트 3단계 — 시드 온톨로지 + 엔티티 자동 추출·업서트 ✅
- 시드 35노트(`vault_seed/entities/`), `core/ontology.py`(추출·정제·병합·인덱스),
  `supabase-ra-vault-setup.sql`(ra_vault 서버 사본), app.py 훅(저장 후 추출→업서트,
  시드 자동 적재, 전체 볼트 zip, 사이드바 볼트 관리 내보내기/가져오기)
- 원칙 구현: 요약 덮어쓰기 금지(사용자 큐레이션 존중), confidence 코드 클램프,
  enum 밖 술어·중복·자기참조 제거, 첨부 run 사실 🔒기밀후보 마킹, 추출 실패는
  삼키고 계속. 상세: [[13 지식 볼트와 온톨로지]] 3단계 구현 상세
- 유닛 검증: 시드 파싱(35노트·관계 68건 enum 준수)·인덱스·추출 정제·별칭 매칭
  업서트·재병합 멱등성·zip 왕복·섹션 소실 내성 전부 통과
- 라이브 검증 완료 (같은 날, 사용자 SQL 실행 후): 시드 36파일 적재 → Gemini
  실조사 → "온톨로지 반영: 신규 2·갱신 3" — EU 배터리 규정·디지털 배터리 여권
  신규, 이차전지·LCA·제품 탄소발자국은 **별칭 매칭으로 기존 시드에 축적**.
  as_of·run 링크·신뢰도 기록, 요약 보존, 관계 중복 없음, 전체 볼트 zip 생성 실측
- 배포: 커밋 `81f4942` (같은 날, 헬스체크 정상 — Secrets 추가 불필요)

### 20. 지식볼트 4단계 — 지식 주입 (자가 교정 루프) ✅
- `core/ontology.py`: find_relevant_entities(정규화 매칭+관계 1홉, 5노트·8k자
  상한)·build_knowledge_block(미검증 라벨·기밀후보 제외)·apply_review_flags
  ('기존 지식과의 차이' 섹션→주입 엔티티 '검토 필요' 절, 이름/별칭 매칭 라인만)
- `core/pipeline.py`: `_brief_block(include_accumulated=False)` — 채점 프롬프트
  에서만 [축적 지식] 제외 (설계상 유일한 pipeline.py 수정, 신규 발견 저평가 방지)
- `app.py`: "🧠 축적 지식 주입" 체크박스(기본 켜짐)+주입 상태 표시,
  instructions에 차이 명시 지시 자동 append, 추출 훅에 순환 인용 금지·검토
  필요 반영("검토 필요 N건" 캡션)
- 검증: 유닛(매칭/1홉/상한/별칭/기밀 제외/채점 제외/검토 필요 매칭/순환 금지
  프롬프트) + 기존 3개 스위트 회귀 + 라이브 e2e(CBAM 재조사 → 4건 주입 →
  아카이브에 주입 증거 → EU CBAM 노트에 사실 3건 축적). 상세: [[13 지식 볼트와 온톨로지]]
- 배포: 커밋 `d0c3d9a` (같은 날, 헬스체크 정상)

### 21. 라이트 모드 — 지식창고 우선 저비용 실행 ✅
- `core/light.py`: run_light_pipeline(경량 모델 1~2회: 키워드 시 웹 조사+구조화,
  아니면 구조화만) — 풀과 동일한 PipelineResult 반환으로 5탭·보고서 3종·
  아카이브·볼트 전부 재사용. 업무 템플릿 5종(검증 4단계 등, 업무패턴 문서 2-2)
- `config.LIGHT_MODEL_DEFAULTS`+`<KEY>_LIGHT_MODEL` env, `build_providers(light=)`.
  **함정: gemini-2.5-flash가 신규 사용자 404** (ListModels에는 있음) →
  실측으로 `gemini-3.5-flash` 확정
- app.py: 사이드바 "실행 모드" 라디오(라이트 기본), 라이트 전용 단일 LLM
  셀렉트(비용 우선순위 gemini→openai→anthropic)·분량 기본 6장, params에
  mode/model/template 기록, **추출 훅이 run params 기준으로 경량 모델 사용**
- 검증: 유닛(모델 해석·팩토리·템플릿·1회/2회 경로·주입 전파) + 라이브 e2e
  (주입 4건→Flash 2회 호출→보고서·출처 4건→아카이브(mode=light)→추출 신규 1·
  갱신 3). 상세: [[14 라이트 모드]]
## 2026-07-12

### 22. 라이트 모드 배포 + Streamlit 핫리로드 장애 복구
- 배포: 커밋 `58e1d1f` push → 익명 헬스체크 303 "정상"으로 보고했으나,
  **라이브 앱은 ImportError로 다운돼 있었음** (사용자 스크린샷으로 발견)
- 원인: 코드는 정상 — Streamlit Cloud 핫리로드가 app.py만 재실행하고 이미
  로드된 **구버전 core.config 모듈을 재사용** → 새 함수 `resolved_light_model`
  import 실패. 대시보드 ⋮ → **Reboot**으로 즉시 복구(첫 화면 렌더 실측)
- 교훈 2건을 deploy 스킬 3단계에 반영:
  ① **Invite-only에선 익명 curl 303이 앱 내부 크래시를 못 잡는다** (303은
  플랫폼 단 응답 — 앱이 죽어 있어도 303이 나옴)
  ② **core/ 모듈에 새 함수·상수를 추가한 배포** 뒤에는 로그인 브라우저로
  첫 화면 확인, ImportError면 재푸시가 아니라 Reboot이 해법
- 이로써 이번 로드맵 전체(지식볼트 1~4단계 + 라이트 모드)가 라이브 완료.
  잔여 백로그: 온디맨드 교차검증 / 클라이언트 컨텍스트 분리(기밀 격리 승격) /
  실행당 비용 표시 / gpt-5-mini 검증(OpenAI 키 등록 시) / 볼트 5단계(GitHub
  동기화·다중 사용자 — 사용자 2인 이상 확정 시)

## 2026-08-04

### 23. Supabase 프로젝트 주소 변경 대응 (볼트·아카이브 전면 실패 복구)
- 증상: 앱 사이드바 "이력 조회 실패"·"볼트 zip 준비 실패" —
  `NameResolutionError: Failed to resolve 'unjhdoulorbmpepnkcpr.supabase.co'`
- 진단: 로컬 PC에서도 동일 실패 + 8.8.8.8로도 미해결 → **Supabase 장애가 아니라
  프로젝트 자체가 사라진 것**. 대시보드 확인 결과 실제 프로젝트는
  **`koorjatscpkvomjosenc`**(STATUS Healthy). 즉 2026-07-04에 옮겨갔던
  `unjhdoulorbmpepnkcpr`이 없어지고 원래 프로젝트로 되돌아온 상태
- 복구: 새 프로젝트에 `ra_runs`·`ra_vault` 생성(SQL 재실행) → `.env` URL 교체 +
  사용자가 새 service_role 키 입력 → 시드 온톨로지 36파일 재적재 → zip 생성까지
  로컬 실측 통과. **배포 앱은 Secrets의 URL이 옛 값 그대로여서 계속 실패** —
  Secrets 두 줄 교체 후 정상화(**Reboot은 Streamlit 쪽이지 Supabase의
  Restart project가 아님** — 사용자가 헷갈린 지점)
- 손실: 옛 프로젝트의 조사 이력 3건·서버 볼트 사본. 시드는 repo에 있어 무손실
- **홈페이지도 동일 문제 → 같은 날 해결**(커밋 `d522fe3`): `admin.html`·
  `assets/site.js`·`assets/editor.js`의 Project URL + publishable(anon) 키를
  새 프로젝트 값으로 교체. 옛 프로젝트 소멸 이후 **상담 폼 저장·관리자 화면이
  동작하지 않던 상태**였음. 새 프로젝트의 RLS는 정상 — 익명 키로 `leads` 조회
  시 빈 배열(service_role로는 2행 확인). `dist/`는 gitignore라 Netlify가 재빌드
- 진단 요령 메모: **로컬 PC에서도 같은 NameResolutionError면 플랫폼 장애가
  아니라 프로젝트 소멸/일시정지**(`supabase.co`는 뜨는데 서브도메인만 안 뜨면
  확실). Supabase 상태 페이지의 "부분 장애" 표시에 낚이지 말 것

### 24. 볼트 왕복 견고화 — zip 폴더명 방어 + Obsidian 태그
- 계기: 사용자가 볼트 zip을 실제로 Obsidian에서 열어보며 "태깅이 없는데 괜찮냐"
- `parse_vault_zip`: 최상위 폴더를 이름 무관 반복 제거 + 볼트 구조 아니면 거부
  (종전엔 폴더명을 바꾸면 어긋난 경로로 **조용히** 저장됨 — 실측으로 확인)
- 노트 태그 `[entity, entity/<타입>, 미검증]` — 태그 패널 탐색 + `tag:미검증`
  검수 워크플로. 코드는 태그를 읽지 않음(기능 무영향), 옛 노트는 tags 줄이
  없을 때만 백필(사용자가 지운 `미검증` 복원 금지)
- 시드 35노트 태그 반영 + 서버 볼트 재적재(축적 사실 0건 상태라 무손실 확인 후)
- 상세: [[13 지식 볼트와 온톨로지]] "볼트 왕복 견고화"

## 2026-08-05

### 25. 비서 기능 2종 — 자동 모니터링·알림 + 볼트 기반 지식 비서
- 사용자 요청: ① 특정 사이트/키워드를 매일 정해진 시각에 점검해 **새로운 내용만**
  요약·볼트 축적하고 카톡·메일로 알림 ② 질문하면 **볼트 내용만으로** 답하는 비서
  ("개인 도서관 + 도서관을 다 아는 사서")
- 신규 모듈: `core/watch.py`(지문 판별·수집·요약) · `core/watch_runner.py`(배선) ·
  `core/notify.py`(SMTP·카카오 memo) · `core/librarian.py`(볼트 검색·근거 강제) ·
  `core/vault_sync.py`(시드 — app.py에서 분리) · `watch_run.py`(CLI) ·
  `.github/workflows/watch.yml`(매시 cron) · `supabase-ra-watch-setup.sql`
- **앱을 멀티페이지로 전환** — `app.py`(조사) + `pages/1_📡_모니터링` +
  `pages/2_📚_지식_비서`, 공통 부트스트랩 `ui_common.py`(비밀번호 게이트를 모든
  페이지가 통과하도록). 상세: [[15 자동 모니터링과 알림]] · [[16 지식 비서]]
- 핵심 판단 3가지
  ① **스케줄은 앱 바깥** — Streamlit Cloud 앱은 접속 중일 때만 살아 있어 "매일
     08시"를 못 지킨다. GitHub Actions가 매시 깨우고 `is_due`가 KST 시각으로 거른다.
     수동/자동이 **같은 `run_watch`** 를 부르게 해 동작 분기를 없앰
  ② **'새로움' 판정은 코드가** — LLM에게 묻지 않고 URL/콘텐츠 해시 지문으로 판정.
     LLM은 이미 확정된 것의 요약만. page 감시 첫 회는 기준선 수집(무알림)
  ③ **비서는 임베딩 없이** — 어휘 매칭 + 엔티티 인덱스(별칭·관계 1홉). 임베딩
     인덱스는 사용자가 Obsidian에서 노트를 고치면 조용히 낡아 "마크다운이 원본"
     원칙과 충돌
- 실측 (2026-08-05, gemini-3.5-flash)
  - 비서: 서버 볼트 36노트 대상 "ESRS E1 vs IFRS S2" → 인용 포함 근거 답변,
    "우리 회사 2027년 매출 전망" → `has_basis:false` + 조사 제안 (환각 없음)
  - 키워드 감시: CBAM 14일 → 3건 수집(96초) → 브리핑(15초) → 노트·메일·카톡 렌더
  - page 감시: 환경부 보도자료 목록 153링크 → 기준선, 재실행 0건 (중복 없음)
  - 브라우저 e2e: 로그인 → 모니터링(테이블 미생성 안내) → 비서 질의 → 근거 노트
    펼침 → '조사하러 가기' → 메인 화면 주제 자동 입력까지 확인
- 실측에서 잡아 고친 결함 2건
  - 제목이 `[기사] …` 이면 `### [[기사] …](url)` 가 되어 **링크가 깨지고 Obsidian이
    위키링크로 오인** → 링크 라벨의 대괄호 치환(`md_link_text`)
  - 검색 LLM이 기사 대신 **언론사 홈 주소만** 주는 경우가 있는데, 그 URL로 지문을
    만들면 같은 매체의 다음 기사가 전부 '이미 본 것'이 되어 조용히 누락됨
    → 경로 없는 URL은 제목 기반 지문으로 전환(`has_deep_path`)
- ⚠️ 사용자 작업 대기: GitHub Actions Secrets 등록 · SMTP/카카오 토큰 발급
  (알림 없이도 볼트 축적은 정상 동작 — 알림 채널은 '미설정'으로 표시)

### 26. 배포 — 그리고 새 함정: `pages/` 추가는 Reboot이 필요하다
- 사용자가 `supabase-ra-watch-setup.sql` 실행 완료 → `ra_watches`·`ra_watch_seen`
  연결 확인 후 배포. 알림(SMTP·카카오)은 사용자 판단으로 보류
- 커밋 `8032699`(병합) — 그 전에 이전 세션의 미커밋 설계서 수정을 `211228a`로 먼저
  보존(홈페이지 Supabase 주소 교체 기록). 홈페이지 repo는 변경 없음
- **함정(신규)**: push 후 로그는 `Pulling code changes` → `Updated app!` 로 깨끗했고
  비밀번호 화면도 정상 렌더됐지만, **`/모니터링` URL이 메인 화면으로 떨어졌다** —
  Streamlit은 **`pages/` 등록을 서버 시작 시에만** 하므로 핫리로드로는 새 페이지가
  안 생긴다. **Reboot 후 사이드바에 app/모니터링/지식 비서 3개가 등록됨**
- 판별법: 배포 후 `<앱URL>/<페이지슬러그>` 를 열어 **사이드바 내비게이션과 탭 제목**을
  확인한다. curl 303도, 게이트 화면이 뜨는 것도 이 문제를 못 잡는다
- **추가 주의**: 비밀번호 게이트(`bootstrap()`)가 `core.*` import보다 먼저 실행되므로
  **게이트 화면이 정상이어도 core 모듈 import 오류는 검증되지 않는다**. 로그인 이후
  화면까지 봐야 완전한 검증(2026-07-12의 ImportError 함정과 같은 계열)
- 참고: 같은 날 14:14 로그에 남아 있던 `KeyError: 'core.pipeline'` 은 옛 app.py:86
  기준 트레이스백 — 이번 배포 이전의 핫리로드 실패 흔적이고, 15:29 갱신분에는 없음

## 2026-08-07

### 27. 다중 사용자 설계 — 옵시디언 볼트 연결부터 테넌트 분리까지 (설계만)

- 요구: "리서치 에이전트를 다른 사람도 쓰게" — 확인 결과 **고객사 대상 유료 서비스**,
  볼트는 **사용자별 완전 분리**. 시작점은 옵시디언(지식볼트) 연결
- 코드 실사로 "2인째에 당일 사고가 나는 지점" 12건을 먼저 확정 → [[17 다중 사용자와 테넌트 분리]]
  - 최악은 `store.vault_replace_all` 의 `DELETE ra_vault?path=like.*` — **전역 삭제**라
    두 번째 사용자가 zip을 올리는 순간 첫 사용자의 볼트가 통째로 사라진다
  - `ra_vault` PK가 `path` 하나(전역 볼트 1개), `ra_runs`·`ra_watches` 도 전부 전역
  - **조용한 유출 경로 2건**: `st.cache_data` 3곳의 캐시 키에 사용자가 없어 테넌트
    컬럼만 붙이면 캐시 히트로 남의 데이터가 렌더된다 / `os.environ` 브리지는 프로세스
    전역이라 사용자별 키(BYO)를 넣는 순간 옆 세션에 샌다(프로바이더가 키를 env에서
    암묵적으로 읽는 것과 같은 뿌리)
- 확정 방향
  - 격리 키는 `user_id` 가 아니라 **`tenant_id`** (1테넌트 1인이면 요구와 동일, 담당자
    추가·인수인계에서 스키마를 안 고침)
  - 격리는 **RLS가 강제**한다 — 대화형은 사용자 JWT+anon, 배치(`watch_run.py`)만
    service_role + tenant 필수 인자. `core/store.py` 는 `Store` 객체로 바꿔
    "테넌트 없이 볼트를 부를 수 있는 함수"를 없앤다
  - 볼트 왕복: **교체 → 3-way 병합**(기준선 `_meta/export.json` 의 `exported_at`),
    tenant 대조로 남의 zip 거부, 병합 전 스냅샷·되돌리기. 내보낸 뒤 쌓인 자동 축적분은
    zip에 없어도 **지우지 않는다**
  - 자동 동기화는 Dropbox/OneDrive **앱 폴더**가 현실적 — 구글 드라이브 양방향은
    `drive.file` 로는 사용자가 만든 노트를 못 읽고, 전체 스코프는 restricted라
    구글 검수·CASA 비용이 붙는다(초기 규모에 부적합)
- `librarian`·`ontology`·`pipeline`·`vault_render` 는 dict만 받는 순수 계층이라
  **변경 없음** — 바꿀 곳은 "볼트를 어디서 불러오는가" 한 겹뿐
- 구현은 미착수. 1단계(스키마·RLS·Store 객체화)를 로그인 붙이기보다 **먼저** 한다 —
  순서를 뒤집으면 데이터가 섞인 채 사용자만 는다

### 28. 판매 모델 전환 — 호스팅형에서 **설치형 패키지**로 (설계만)

- 사용자 제안: "독립 파일 패키지로 판매. 각자 PC에 설치하고 **LLM·옵시디언을 본인
  계정으로**. 키는 화면에서 등록하고 볼트는 자동 생성" → [[18 설치형 패키지]]
- 평가: **이쪽이 맞다.** 27번에서 잡은 결함 12건 중 **10건이 착수 없이 소멸**한다
  - 볼트 왕복·병합·충돌·스냅샷 설계 전체가 **"Streamlit Cloud 파일시스템이
    휘발성"이라는 한 가지 제약**에서 나온 것이었다. 로컬이면 볼트가 진짜 폴더가
    되고 Obsidian이 외부 변경을 즉시 집어 든다 — **동기화 개념 자체가 사라진다**
  - [[10 배포와 홈페이지 연동]]의 "불변 리스크"(대표 키로 작동 → 무제한 청구)가 소멸.
    사용량 한도·BYO 키·결제 설계가 통째로 불필요
  - 고객사 자료가 우리 서버를 안 거친다 → ESG 도메인에서 **기능이 아니라 영업 자산**
  - `.research-agent/` 를 **볼트 폴더 안**에 두면 볼트 전환 = 아카이브·감시까지 전환.
    문서 13 안전장치 3(고객사 기밀 격리)이 마킹이 아니라 **폴더 경계로 완결**
- 대신 비용이 인프라·원가에서 **배포·지원·업데이트**로 옮겨간다 (18의 2절에 명시)
  - **PyInstaller onefile 비권장** — Streamlit 자산 수집이 까다롭고 국내 백신 오탐이
    잦다. Inno Setup 인스톨러 + 런타임 폴더 권장
  - **유일한 기능 후퇴**: 감시 정기 실행이 PC 전원에 묶인다. 게다가
    [watch.py:115](core/watch.py:115) `is_due` 는 `now.hour` 가 **정확히 일치**할 때만
    실행하므로(매시 깨우는 클라우드 스케줄러 전제), 08시 감시인데 10시에 켜면 그날은
    건너뛴다 → **놓친 시각 따라잡기** 로직이 필요
  - API 키 발급이 ESG 실무자에겐 진입장벽 → 온보딩은 **Gemini 무료 티어부터**
    (라이트 모드가 기본이라 Gemini 키 하나로 전 기능 동작)
- `pipeline`·`librarian`·`ontology`·`vault_render`·`reports` **변경 없음** —
  Streamlit 비의존 원칙([[02 파이프라인 설계]])이 여기서 값을 했다. 바꿀 곳은
  `store.py`(Supabase→폴더+sqlite3)와 신규 설정·런처 계층
- 0단계로 **패키징 스파이크 1주**(깨끗한 Windows에서 설치→실행→조사 1건)를 먼저 둔다 —
  유일한 미지수라 여기 결과로 나머지 로드맵이 확정된다
- [[17 다중 사용자와 테넌트 분리]]는 **보류**(폐기 아님). 팀이 하나의 볼트를 함께
  키우는 요구가 나오면 재개

### 29. 판매 구조 확정 — 체험판 + 정식판, 자동 업데이트, 복제 방지 (설계만)

- 사용자 지시 3건이 이어짐: ① **2개 상품**(현행 호스팅=영업용 체험판, 설치판=정식판)
  ② 홈페이지에서 다운로드 + **패치 자동 업데이트** ③ **구매자 외 사용 불가**
  → [[19 두 제품 구성과 자동 업데이트]] · [[20 라이선스와 복제 방지]]
- **새로 지을 인프라가 거의 없다** — 매니페스트는 홈페이지(Netlify)에 JSON 파일 하나,
  설치 파일은 GitHub Releases, 릴리스는 [[11 원커맨드 배포]]에 빌드 잡 하나 추가
- **체험→구매 이음매가 이 전략의 심장**: 체험에서 쌓은 볼트를 잃으면 구매가 막힌다.
  [[18 설치형 패키지]]에서 "불필요"라고 했던 **zip 왕복 코드가 여기서 되살아난다** —
  상시 동기화(폐기) → **1회성 이주 도구**(유지). `build_full_vault_zip`·
  `parse_vault_zip`·`build_index` 가 그대로 쓰여 구현 비용이 거의 0
- 체험판 격리는 **[[17 다중 사용자와 테넌트 분리]]의 축소판**(`trial_code` 컬럼 + 필터,
  RLS 없음)으로 충분 — 체험엔 기밀을 올리지 않는다는 전제. 다만 17의 `st.cache_data`
  캐시 키 유출 경로는 **체험판에서도 그대로 유효**하므로 반드시 함께 고친다.
  17이 통째로 버려지지 않고 여기서 재사용되는 셈
- **복제 방지 방향 전환**: 18번 문서에서 "복제 방어는 포기" 라고 썼던 것을 철회.
  설치판은 **파이썬 소스가 그대로 담기므로** 클라이언트 내부 검사는 무의미
  (`if license_ok:` 한 줄 삭제로 끝) → 라이선스를 **"실행 허가"가 아니라
  "프롬프트 팩 인출 권한"** 으로 재정의. 이 제품의 값어치는 코드가 아니라 프롬프트·
  스키마·시드에 있으므로, 팩이 없는 복제본은 **조사를 시작조차 못 한다**
  - 서버로 가는 것은 라이선스 키·기기 지문 해시뿐 — **고객사 자료 미전송 원칙 유지**
  - 팩은 릴리스마다 회전 → 자동 업데이트가 곧 복제 방어가 된다
  - 남는 한계(유효 라이선스로 활성화 후 메모리 덤프)는 숨기지 않고 문서에 명시
- **프롬프트 팩 분리를 1단계로 앞당긴다** — 나중에 하면 전 모듈을 다시 헤집는다.
  라이선스와 무관하게도 값이 있다(앱 재배포 없이 ESG 규제 변화에 프롬프트만 갱신)
- 로드맵에서 **체험판 개조(2단계)를 정식판보다 먼저** 둔다 — 작업량이 적고 끝나는
  즉시 영업을 시작할 수 있다. 그 반응이 이후 우선순위를 정한다

### 30. 휴대폰 사용 — "볼트를 클라우드에 두면 되나?"에 대한 답 (설계만)

- 질문: 폰에서도 비서를 쓰려면 지식볼트만 구글/원드라이브에 두면 되는지
  → [[21 휴대폰에서 쓰기]]
- **답: 절반만 해결된다.** 클라우드 저장소는 **파일 동기화를 주지 연산을 주지 않는다**.
  비서는 [`librarian.ask()`](core/librarian.py:236) 가 볼트를 스캔해 LLM을 호출하는
  **프로그램**이라, 폰에는 코드도 사용자 키도 프롬프트 팩도 없다
  - 폰에서 **읽기·검색**: 된다 (단 "구글 드라이브에 넣으면 끝"은 아님 — iOS는
    온디맨드 다운로드 때문에 신뢰 어렵고, 확실한 건 Obsidian Sync(유료) 또는
    Remotely Save 플러그인. **실기기 확인 후 매뉴얼에 날짜와 함께 기재**)
  - 폰에서 **알림**: **이미 된다** (이메일·카톡 — [[15 자동 모니터링과 알림]])
  - 폰에서 **비서 질의**: PC의 정식판에 브라우저로 접속하면 된다
- **정식판이 이미 웹앱(Streamlit)이라는 게 결정적** — 새로 만들 모바일 앱이 없다.
  설정에 "휴대폰 접속" 토글(0.0.0.0 재기동 + QR + PIN 강제) 하나면 된다
  - 접속은 **같은 Wi-Fi 기본 / 외부는 Tailscale**. 포트 포워딩은 금지
  - **폰 접속은 좌석을 소모하지 않는다** (폰은 브라우저일 뿐) → 판매 문구로 쓸 만함
- **[[18 설치형 패키지]]에서 지우려던 비밀번호 게이트를 되살린다** — 휴대폰 접속을
  켜는 순간 "localhost라 안전"이라는 전제가 깨진다. `require_password` 를 PIN
  게이트로 재사용(PIN 미설정이면 접속이 안 켜지는 fail-closed).
  체험판 이주에서 zip 코드가 되살아난 것과 같은 패턴 — **지우려던 코드가 두 번째로
  다른 역할을 얻었다**
- "PC가 꺼져 있어도"는 결국 호스팅이라 18번의 판매 논리와 충돌 → 기본 제공 안 함.
  수요 확인 시 **옵트인 유료 옵션**으로만 열고, 그때 [[17 다중 사용자와 테넌트 분리]]의
  테넌트 설계를 꺼내 쓴다 (보류 문서가 다시 쓰일 자리)

### 31. 설치판 구현 문서 착수 — 아키텍처 + 단계별 계획

- 사용자 결정: **판매 가능한 개인 설치형을 먼저 만든다.** 18~21이 "무엇을 팔 것인가"
  였다면 여기서부터 구현 문서 → [[22 설치판 아키텍처]] · [[23 설치판 구현 계획]]
- 구조 핵심
  - **디스크 3영역 분리** (프로그램 / 사용자 데이터 / 볼트). 업데이트가 사용자
    데이터를 못 건드리게 하는 장치. `.research-agent/`(sqlite·충돌본)를 **볼트 안**에
    둬 볼트 폴더 하나만 복사하면 이력·감시까지 이전됨
  - **Program Files에 설치하지 않는다** — 거기 쓰려면 관리자 권한이 필요해
    **자동 업데이트마다 UAC가 뜬다**. per-user 설치(`PrivilegesRequired=lowest`)
  - **경계 셋**(`store`·`keys`·`packs`)만으로 두 에디션이 갈린다. 이걸 넘는 분기는 반려
  - `LocalStore` 는 현행 `store.py` **인터페이스를 그대로** 유지 → 호출부 18곳이 거의 무변경
  - 신규 고려 2건: 볼트 전수 읽기가 화면당 여러 번 일어나므로 **mtime 증분 캐시** 필요
    (Supabase일 땐 REST 1회였음) · 앱과 작업 스케줄러가 같은 sqlite를 열므로 **WAL 필수**
  - **원자적 쓰기 + mtime 충돌 감지** — Obsidian이 연 파일을 반쪽으로 읽거나
    사용자 편집을 덮어쓰는 사고를 둘 다 막음. 자동 병합은 하지 않음(판정은 사람)
  - **상태별 잠금 표**를 미리 확정 — 어떤 실패에도 **볼트 열람·내보내기는 안 막는다**
    (고객 데이터를 인질로 잡지 않는다). 활성화 서버 장애는 **유예 처리**
  - **네트워크 아웃바운드 전표** 작성 — 보안 검토에 그대로 제출 가능. 이 표에 없는
    아웃바운드가 생기면 리뷰 반려
- 계획 핵심
  - **0단계 패키징 스파이크(1주)가 선행 필수** — 유일하게 실측이 없는 미지수.
    한글 사용자명 경로·국내 백신 통과까지 포함. 실패해도 배포 형태만 바뀌고 1~7단계 무영향
  - **프롬프트 팩 분리(2단계)를 라이선스보다 먼저.** 나중에 하면 전 모듈을 다시 헤집는다.
    조립 함수는 두고 **문자열 리터럴만** 치환하며, DoD에 **분리 전후 프롬프트
    바이트 단위 동일** 비교를 넣어 회귀를 막는다
  - **판매 최소선은 0~7단계.** 8단계(모니터링 로컬 스케줄)는 없어도 판매 가능
  - 상시 회귀 6종(R1~R6) — 특히 **R1 체험판 정상 기동**: 영업 도구가 죽으면 안 되므로
    Supabase 경로를 지우지 않는다. 이 작업은 교체가 아니라 **분기 추가**
- 아직 코드 변경 없음. 다음 행동은 **0단계 스파이크**

### 32. 설치판 0단계 — 패키징 스파이크 실측 (첫 코드 산출물)

- **결론: 임베디드 파이썬 방식 확정.** 배포 형태를 바꿀 이유 없음 → [[23 설치판 구현 계획]]
- 산출물 `packaging/` — `build.ps1` · `launcher.py` · `installer.iss` · `app.ico` · `VERSION`
- 실측: **설치 파일 73MB** · 설치 62초 · 디스크 377MB(다이어트 전 533MB) ·
  **첫 기동 3.1초** / 2회차 1.9초
- 라이트 조사 1건 e2e 성공(Gemini), 보고서 3종 생성, 한글 경로 정상,
  `pages/` 3개 등록 확인(26번 판별법), **Supabase 없이도 기능 저하 없음**
- **시스템 파이썬 격리 확인** — `sys.path` 3개가 전부 런타임 내부.
  파이썬 없는 PC에서 도는 것의 직접 증거
- 함정 7건 (상세는 설계서 23)
  - **MAX_PATH(260자)로 빌드가 실패**했다 — 롱패스 꺼진 기본 상태 + 긴 작업 경로.
    실제 설치 위치 기준으로는 199자로 여유 61자 → 설치 경로를 더 깊게 만들지 않는다
  - **`.ps1` 을 UTF-8 BOM으로 저장해야 한다** — 5.1이 BOM 없으면 ANSI로 읽어 한글
    주석이 깨지고 구문 오류. [[11 원커맨드 배포]]의 한글 이슈가 빌드 스크립트에서 재발
  - `google-api-python-client` **98MB(전체의 18%)** 제거 — Google Slides 엔진 전용이고
    그 엔진은 ImportError를 잡아 안내를 내므로 기능 저하 없음
  - **빌드가 `.streamlit/` 를 통째로 복사하면 로컬 `secrets.toml` 이 고객 PC로 나간다**
    → `config.toml` 만 담고, 산출물 비밀 파일·키 패턴 스캔을 빌드 실패로 연결
  - 그 스캔 첫 판이 `certifi\cacert.pem` 을 오탐 → 검사 범위를 앱 영역으로 한정
  - **런처 exe를 컴파일하지 않기로 결정** — 바로가기가 `pythonw.exe`로 `launcher.py`를
    부르고 아이콘만 씌운다. 컴파일 산출물이 없으면 백신 오탐 여지가 준다
  - **설치 후처리로 바이트코드를 선컴파일한다** — 없으면 진짜 첫 실행이 **10.1초**.
    `compileall -j 0` 15초를 쓰면 3.1초로 떨어지고, `.pyc` 는 설치 시 생성되므로
    **다운로드 용량은 그대로 73MB**. 측정 함정도 함께 기록: 빌드 직후 스모크 테스트를
    돌리면 캐시가 덥혀져 "4.3초"로 잘못 보인다 — **콜드스타트는 갓 설치한 상태에서 잰다**
- 인스톨러(Inno Setup 6.7.3, 사용자 승인 후 `/CURRENTUSER` 설치)까지 실측 완료
  - 무인 설치·제거 종료 코드 0, 시작 메뉴 바로가기, Edge 앱 모드 창 정상
  - **제거 후 프로그램은 완전 삭제되고 볼트·`agent.db`·`config.json` 은 남는다** —
    [[22 설치판 아키텍처]] 2절 3영역 분리가 의도대로 동작
  - Windows Defender 검사 위협 없음. **국내 백신(알약·V3)은 미검증** — 배포 전 확인 필요
  - `dist/` 를 `.gitignore` 에 추가 (73MB 인스톨러가 저장소에 들어갈 뻔했다)

### 33. 설치판 1단계 — 저장 계층 분리 (Supabase / 로컬 폴더+sqlite)

- 산출물: `core/store_local.py`(LocalStore·VaultCache) · `core/store_supabase.py` ·
  `core/store.py`(해석기로 축소) · `core/runs.py` · `core/appdirs.py` ·
  `core/settings.py` · `core/edition.py` · `tests/test_store_local.py`
- **호출부를 거의 안 건드린 방법**: 저장소를 `store` 라는 이름의 **객체**로 받는다
  (`store = store_mod.active()`). app.py 12곳·pages 6곳의 `store.vault_list()` 가
  한 줄도 안 바뀌었고, 동시에 **모듈 수준 저장 함수가 사라져** "어느 볼트인지
  정하지 않고 볼트를 부르는" 코드는 이제 작성할 수 없다
  (→ [[17 다중 사용자와 테넌트 분리]]가 요구한 성질을 여기서 확보)
- 검증
  - `tests/test_store_local.py` **36항목 전부 통과** (한글·공백 경로에서)
  - **정식판**: Supabase 환경변수를 **지운 채** 앱 기동 → 지식 비서 방문만으로
    볼트 폴더에 **시드 35노트 자동 생성**(한글 폴더·태그·별칭·인덱스) →
    Gemini로 "CBAM이 뭐야?" 질의에 `[[EU CBAM]]`·`[[EU ETS]]` 인용과 관계 1홉
    확장까지 정상 답변. **Obsidian이 바로 열 수 있는 진짜 폴더**가 됐다
  - **R1 체험판 무회귀**: 같은 코드로 Supabase 경로 정상 — 사이드바 이력 표시,
    볼트 37파일 조회, 3개 화면 모두 렌더
  - 에디션 판정은 `RA_EDITION` > `<설치폴더>/version.json` > 기본 trial →
    **저장소에서 그냥 실행하면 지금까지와 똑같이 동작**한다
- 이번에 새로 막은 것
  - **캐시 키에 볼트를 넣었다** (`_list_runs_cached`·`_watches`·`_vault`) —
    없으면 볼트를 바꿔도 옛 목록이 캐시에서 나온다 (문서 17 3.7절의 유출 경로)
  - **사용자 편집을 이기지 않는다** — mtime이 달라졌으면 덮어쓰지 않고 앱 버전을
    `.research-agent/conflicts/` 에 보관. 자동 병합은 안 한다
  - **경로 탈출 차단**(zip-slip) — 4단계 체험판 볼트 가져오기가 이 방어를 그대로 쓴다
  - **전체 교체가 조사 이력을 죽이지 않는다** — `vault_replace_all` 이
    `.research-agent/`·`.obsidian/` 을 건드리지 않음을 테스트로 고정
- `watch_run.py --vault` 추가 (작업 스케줄러가 볼트를 지정해 호출)

### 34. 설치판 2단계 — 프롬프트 팩 분리 (문구를 코드 밖으로)

- **골든 스냅샷을 먼저 뜨고 분리했다.** `tests/prompt_golden.py` 가 고정 입력으로
  프롬프트·스키마·구성표 **62항목**을 만들어 저장하고 분리 후 바이트 단위로 비교한다.
  LLM을 안 부르므로 빠르고 결정적. **이 순서가 아니었으면 "비슷해 보이는데 다른"
  프롬프트를 못 잡았다**
- 옮긴 것: `pipeline`(조사·토론·채점·종합 프롬프트, 분량 계획 문구, 채점 기준 9종,
  보고서 스키마) · `ontology`(추출 프롬프트·스키마, 타입 6종, 술어 12종, 축적 지식
  지시문) · `librarian`(시스템·답변 프롬프트·스키마) · `watch`(검색·정리·다이제스트,
  스키마 2종) · `light`(업무 템플릿 5종, 구조화 프롬프트)
  → `core/prompts/pack.json` (prompts 32 · schemas 5 · config 8), 접근은 `core/packs.py`
- 설계 판단 셋
  - **치환자는 `string.Template`(`$name`)** — 프롬프트에 JSON 스키마 예시 같은
    중괄호가 섞이므로 `str.format` 은 위험. 본문에 `$` 가 없음을 먼저 확인했다.
    `safe_substitute` 가 아니라 `substitute` — 못 채운 치환자가 조용히 남지 않고 터진다
  - **임포트 시점에 팩을 읽지 않는다** — 팩이 없어도 앱은 뜨고 볼트 열람은 계속돼야
    하므로. PEP 562 모듈 `__getattr__` 로 접근 순간에 꺼낸다(프록시가 아니라 진짜
    dict/list 라 SDK 에 그대로 넘어간다). 대신 호출부를
    `from core import pipeline` + `pipeline.REPORT_SCHEMA` 로 바꿨다
  - **조립 로직은 코드에 남겼다** — 팩은 문구를, 코드는 어디에 끼울지를 안다.
    분량 계획도 개수 산식은 코드, 서술 문구는 팩으로 갈랐다
- 검증: 골든 **62항목 달라짐 0** · Gemini 라이트 실조사 e2e 성공(섹션 3·표 1·출처 2) ·
  **팩 제거 시 볼트 쓰기/읽기는 그대로 동작**하고 안내 문구 있는 PackError ·
  1단계 테스트 36항목 재통과
- 부수 효과: **앱 재배포 없이 프롬프트를 갱신**할 수 있게 됐다 — ESG 규제가 매년
  바뀌는 도메인에서 라이선스와 무관하게도 값이 있다

### 35. 설치판 3단계 — 키·설정 화면 (OS 키체인)

- 산출물: `core/keys.py` · `pages/0_⚙️_설정.py` · `providers.verify_key()` ·
  프로바이더 3사 생성자 `api_key` 인자 · `config.api_key_for()`
- **SDK 가 환경변수를 암묵적으로 읽게 두지 않는다** — `api_key_for()` 로 꺼내
  생성자에 넘긴다. 그래야 화면에서 등록한 키가 **재시작 없이** 반영되고, 사용자
  키가 프로세스 전역(`os.environ`)에 올라가지 않는다 (문서 17 3.5절의 선결 조건)
- **키체인 백엔드가 없으면 조용히 환경변수로 폴백** — 체험판(리눅스 헤드리스)에는
  백엔드가 없으므로 같은 코드가 두 에디션에서 다 돌려면 필수.
  `keyring.backends.fail.Keyring` 인지 확인해 "있는 척하는 백엔드"를 걸러낸다
- 온보딩은 **Gemini 먼저** (무료 티어 → 카드 없이 시작, 라이트 모드라 키 하나로 전 기능)
- 검증(패키지 런타임): 키체인 왕복·**os.environ 무오염** · **키 없이 띄운 앱이
  외부 등록 키를 재시작 없이 인식** · 잘못된 키 즉시 400 반환 · 프로바이더별 독립
- **실측으로 잡은 버그 2건**
  - **`version.json` BOM** — PowerShell `Out-File -Encoding utf8` 이 BOM을 붙여
    `json.loads` 가 조용히 실패, **버전이 늘 `dev`** 였다. 설정 화면에 "버전 dev"가
    떠서 발견. 표시만의 문제가 아니라 **6단계 자동 업데이트의 버전 비교가 통째로
    망가지는** 버그였다. 읽는 쪽 `utf-8-sig` + 굽는 쪽 BOM 제거로 양쪽 수정
  - **2단계에서 흘린 `ontology._norm` 유실** — 팩 분리 때 리터럴 블록과 함께 삭제됨.
    골든 테스트는 프롬프트만 보므로 못 잡았고, 주입 실패를 안전장치가 삼켜
    **조사는 성공한 채 지식만 조용히 주입 안 되는** 상태였다.
    2단계 이전 커밋과 최상위 정의 목록을 기계 비교해 유실 범위 확정(그것 하나뿐),
    재발 방지로 `tests/test_core_smoke.py` **26항목** 신설 —
    LLM 없는 순수 로직(병합·인덱스·주입 매칭·검토 필요 반영·사서 검색·감시 지문·
    zip 왕복)을 전부 실행한다. 골든 테스트의 사각지대가 정확히 거기였다

### 36. 설치판 4단계 — 최초 실행 마법사 · 볼트 자동 생성

- 목표: **"설치하면 볼트가 자동으로 생긴다"** (→ [[23 설치판 구현 계획]] 4단계)
- 산출물: `core/vault_setup.py`(로직) · `ui_vault.py`(마법사·관리 화면) ·
  `tests/test_vault_setup.py` 69항목
- 볼트가 없으면 **첫 화면이 마법사로 바뀐다** — 키보다 볼트를 먼저 묻는다.
  볼트는 이 제품이 무엇인지 설명해 주지만, API 키는 설명 없이 요구하면 벽이 된다
- `.obsidian/` 프리셋 동봉 — 그래프 뷰(엔티티 종류별 색 5그룹) · **태그 패널을
  펼친 채로**. **이미 `.obsidian/` 이 있으면 손대지 않는다** (사용자 설정 보호)
- 폴더 진단 `inspect()` — 새 폴더 / 빈 폴더 / 기존 볼트 루트 / **남의 볼트 안쪽**을
  구분하고, 설치 폴더 안·MAX_PATH 초과는 막고, 클라우드 동기화 폴더는 경고만 한다
- 폴더 선택 창: 임베디드 파이썬에 tkinter 가 없어 **PowerShell FolderBrowserDialog**.
  명령은 Base64(UTF-16LE)로 넘긴다 — 한글·인용부호가 콘솔 코드페이지를 안 타게
- 체험판 볼트 zip 가져오기: 노트는 폴더로, `runs/*.run.json` 은 **sqlite 로**.
  run.json 을 폴더에도 두면 둘이 어긋났을 때 어느 쪽이 진짜인지 답할 수 없다
- **실측으로 잡은 것 3건** (아래 4단계 실측 결과 참조) — Obsidian URI · 색인 유실 ·
  마법사 마지막 화면 소멸
- 검증: 패키지 런타임에서 마법사 e2e(볼트 36노트 · Obsidian 1.13.4 실제 열림 ·
  태그 패널 표시) · zip 가져오기(노트 3 + 이력 1건 복원) · 볼트 추가·전환 ·
  **R1 체험판 무회귀**(Supabase 이력·볼트 37노트 정상)
- 회귀 세트 4종: 골든 62 · LocalStore 36 · 스모크 26 · **볼트 69** 전부 통과

### 37. 설치판 5단계 — 런처·휴대폰 접속·진단

- 목표: **남에게 건넬 수 있는 물건** (→ [[23 설치판 구현 계획]] 5단계)
- 산출물: `core/mobile.py` · `ui_mobile.py` · `tests/test_mobile.py` 37항목 ·
  런처 `--restart`
- **🚨 치명적 버그 — 정식판이 아예 열리지 않았다.** `require_password()` 가
  에디션을 가리지 않아 `APP_PASSWORD` 없는 설치판이 "Streamlit → Manage app →
  Secrets 에 넣으세요"로 막혔다. 고객이 따를 수 없는 지시다.
  4단계까지 내가 늘 `APP_PASSWORD` 를 넣고 띄워 가려져 있었다 —
  **내 환경변수가 가린 고장**. 인스톨러를 넘겼으면 첫 실행에서 전원이 막혔다
- 게이트를 에디션별로 갈랐다: 체험판=비밀번호(그대로) / 정식판·127.0.0.1=없음 /
  정식판·0.0.0.0=**PIN 필수**
- 휴대폰 접속의 fail-closed **세 겹**(화면·런처·앱). 런처는 설정만 보지 않고
  **키체인의 PIN 을 다시 확인**한다 — config.json 은 손으로 고칠 수 있지만
  키체인은 그렇게 못 만든다. 판정이 터져도 127.0.0.1
- 브루트포스 잠금은 **프로세스 전역**(5회/120초). 세션 상태면 재접속으로 초기화된다
- QR 은 **비서 화면**으로 보낸다 (휴대폰으로 오는 목적이 그것)
- 모바일 레이아웃은 375px 실측 결과 이미 멀쩡해서 **손대지 않았다**
  (사이드바 자동 접힘·컬럼 적층·가로 넘침 0)
- 진단 내보내기 — 키 값·노트 내용 제외, 키 패턴 스크럽
- 체험판 사이드바의 "⚙️ 설정" 제거 (4단계 잔여 항목) — `showSidebarNavigation=false`
  + `ui_common.nav()`
- 검증: 토글 ON→재시작→0.0.0.0→PIN 게이트(LAN·루프백 양쪽)·틀린 PIN 거부 /
  OFF→127.0.0.1→**LAN 연결 거부·루프백 정상** / R1 체험판 무회귀(내비 3개, 설정 없음)
- 회귀 5종: 골든 62 · LocalStore 36 · 스모크 26 · 볼트 69 · **휴대폰 37** 통과

### 38. 설치판 6단계 — 자동 업데이트 (패치가 고객 PC 까지)

- 목표: 패치가 고객 PC 까지 간다 (→ [[23 설치판 구현 계획]] 6단계 · [[19 두 제품 구성과 자동 업데이트]] 4절)
- 산출물: `core/updates.py` · `packaging/updater.py` · `ui_update.py` ·
  `build.ps1` 의 델타 zip·매니페스트 생성 · `tests/test_updates.py` 60항목
- **로컬 릴리스 서버를 세워 0.1.0 → 0.2.0 전 과정을 실제로 돌렸다.**
  사이드바 알림 → 매니페스트 재조회 → 176KB 델타 → sha256 검증 → 앱 종료 →
  `app`↔`app.bak` → 전개 → 재시작 → 헬스 자가진단 → 백업 삭제.
  볼트 35노트·`agent.db`·설정 표식 모두 그대로
- **실측으로 잡은 버그 5건** (단위 시험으로는 안 잡히는 것들)
  1. **업데이터가 자기 자신을 죽였다** — `taskkill /T` 는 트리를 죽이는데
     업데이터가 앱의 자식이었다. `/T` 제거 + `cmd /c start` 로 트리 밖에서 기동.
     ⚠️ **5단계 재시작 버튼에도 같은 결함이 있었다** — 그때는 PowerShell 로만
     눌러 봐서 드러나지 않았다
  2. **업데이터의 cwd 가 `app\`** 이었다(서버 cwd 를 물려받음). 자기가 선 폴더는
     이름을 못 바꾼다 — 프로세스를 다 죽여도 `WinError 32` 가 안 사라지던 원인
  3. **캐시된 판정으로 내려받아** 낡은 sha256 이 "위변조" 헛경보를 냈다 →
     적용 직전에는 매니페스트를 다시 받는다. 반대 방향(낡은 해시가 맞아떨어져
     옛 버전을 새 버전으로 믿는 것)이 더 위험하다
  4. **델타가 버전 파일을 못 갈아 끼웠다** — `version.json` 이 설치 루트에만 있어
     같은 업데이트를 영원히 다시 권했다 → `app\` 안에도 두고 그쪽 우선
  5. **판정 캐시가 업데이트 직후에도 "새 버전 있음"** → 판정 대신 매니페스트를
     캐시하고 읽을 때마다 다시 판정
- 안전 규칙: https 만 · sha256 필수(틀리면 파일도 삭제) · 전개 전 zip 전수 검사
  (zip-slip·`app.py` 없으면 거부) · 중단 시 런처가 첫 기동에서 자동 복구 ·
  **조사 중에는 재시작 금지**(파일 표식, `finally` 로 반드시 해제, 6시간 뒤 만료)
- 검증: 0.1.0→0.2.0 실제 업데이트 · sha256 불일치 거부 · **교체 중단 상태를
  인위로 만들어 자동 복구 확인** · 볼트/설정 보존 · R1 체험판 무회귀
- 회귀 6종: 골든 62 · LocalStore 36 · 스모크 26 · 볼트 69 · 휴대폰 37 · 업데이트 60

### 39. 설치판 7단계 — 라이선스 (판매 가능 지점)

- 목표: 구매자만 실행 가능 (→ [[20 라이선스와 복제 방지]] · [[23 설치판 구현 계획]] 7단계)
- 산출물: `core/licensing.py` · `ui_license.py` · `supabase-ra-license-setup.sql` ·
  `supabase/functions/license/index.ts` · `packaging/make_signing_key.py` ·
  `packaging/make_pack.py` · `tests/test_licensing.py` 56항목
- **라이선스를 "실행 허가"가 아니라 "팩 인출 권한"으로** 만들었다. 빌드에서
  프롬프트 팩과 ESG 시드를 빼므로, 복제본은 지울 검사가 없는 대신
  **조사를 시작할 부품 자체가 없다**
- 기기 바인딩을 **서로 독립인 두 겹**으로: ① 캐시 암호화 키를
  `키체인 비밀 × 기기 지문` 에서 파생(폴더 복사 차단) ② **서명 대상에 기기 지문**
  (키체인까지 옮겨도 검증 실패). 둘 다 시험으로 고정
- **설계에서 바꾼 것**: 팩을 JSON 객체가 아니라 **base64 바이트 그대로** 주고받아
  서명한다. 클라이언트가 JSON 을 재직렬화해 서명을 맞추게 하면 Deno/파이썬의
  표기 차이 하나로 정당한 고객의 활성화가 통째로 실패한다
- 구매자를 막지 않는 쪽도 실측: 서버를 **완전히 내린 채** 조사 계속 동작 ·
  갱신 시점 경과 시 `grace`("사용에는 지장이 없습니다") · 만료 시 조사만 잠기고
  **볼트 열람·내보내기는 그대로** · **서버가 죽어도 기기 해제는 로컬에서 완료**
- 서버로 나가는 필드가 `license_key`·`device_fp`·`device_label`·`app_version`
  넷뿐임을 시험으로 고정 (조사 내용·볼트는 애초에 전송 대상이 아니다)
- 부수: **시드 온톨로지 35노트도 팩에 실었다** — 패키지에 남겨 두면 설계서 20 이
  "값어치"로 꼽은 자산이 잠기지 않고 나간다
- 검증: 잠긴 설치본에서 조사 버튼 비활성·볼트 관리 노출 → 화면에서 활성화 →
  팩 인출·조사 해제까지 실측 · 캐시 파일이 암호문임을 확인 · R1 체험판 무회귀
  (라이선스 문구 노출 0)
- 회귀 7종: 골든 62 · LocalStore 36 · 스모크 26 · 볼트 69 · 휴대폰 37 ·
  업데이트 60 · **라이선스 56**
- ⚠️ **아직 배포하지 않았다** — SQL 실행·Edge Function 배포·시크릿 설정·서명
  키쌍 생성은 승인 후. `/admin/` 발급 화면은 홈페이지 저장소라 미착수

### 40. 설치판 8단계 — 모니터링 로컬 스케줄 (놓친 시각 따라잡기)

- 목표: PC 가 꺼져 있어 놓친 감시를 그날 안에 따라잡는다
  (→ [[23 설치판 구현 계획]] 8단계)
- 산출물: `core/scheduler.py` · `core/watch.py` 의 `is_due` 재설계 ·
  모니터링 페이지 "이 PC 에서 자동 실행" 절 · `tests/test_schedule.py` 30항목
- 새 규칙: **오늘 지나간 마지막 예정 시각 이후로 아직 안 돌았으면 실행한다.**
  어제 것까지 따라잡지는 않는다 — 새벽에 갑자기 도는 것보다 예측 가능한 쪽을 골랐다.
  24시간 매시 깨워도 08,18 감시는 정확히 2번 도는 것을 시뮬레이션으로 고정
- **실측으로 잡은 버그 2건 — 둘 다 조용한 고장**
  1. **`watch_run.py` 가 설치판에서 한 번도 돈 적이 없었다.** 임베디드 파이썬은
     `._pth` 로 `sys.path` 를 고정해 스크립트 폴더가 자동으로 안 들어간다.
     앱은 `streamlit run` 이 대신 넣어 줘 멀쩡했지만, 스케줄러가 부르는 이
     진입점은 `No module named 'core'` 로 죽었고 **콘솔 없는 pythonw 라 흔적도
     없었다.** 0단계부터 인스톨러가 등록해 온 작업이 내내 실패하고 있었다
     → 자기 폴더를 sys.path 에 넣고 `logs/watch.log` 를 남긴다
  2. **"새 항목 없음" 일 때 최근 점검 시각이 기록되지 않았다** (1단계에서 `_mark`
     의 `store` 인자 누락 + 예외 삼킴). 예전 규칙에선 표시 문제였지만 따라잡기는
     이 시각으로 판단하므로, **조용한 감시가 매시 다시 도는 고장**이 됐을 것이다.
     따라잡기를 켜기 전에 고쳤다
- 설계 판단: 등록·삭제는 `schtasks`, **상태 조회는 PowerShell**
  (`schtasks /Query` 출력은 한국어 Windows 에서 번역되어 필드명 매칭이 깨진다)
- 검증(실제 작업 스케줄러): 어제로 되돌린 08시 감시를 22:14 에 **따라잡기 성공** ·
  직후 중복 없음 · `--all` 두 프로세스 **같은 초 동시 실행** 시 sqlite 잠금 오류 0 ·
  작업 이름이 인스톨러와 일치하는지 테스트로 고정 · R1 체험판 무회귀(스케줄 절 비노출)
- 회귀 8종: 골든 62 · LocalStore 36 · 스모크 26 · 볼트 69 · 휴대폰 37 ·
  업데이트 60 · 라이선스 56 · **스케줄 30**
- ⚠️ 정각 자동 기동은 미확인(수동 트리거로만 검증), 설치 옵션 체크박스 경로 미검증

### 41. 배포 준비 ① 라이선스 서버 — 배포 전에 서명 호환성을 못 박다

- 요청: 남은 배포 항목을 1번부터 순서대로 (1 라이선스 서버 · 2 릴리스 파이프라인 ·
  3 `/admin/` · 4 최종 검증)
- **이 PC 에서는 배포를 완료할 수 없다** — Supabase CLI 미설치, 액세스 토큰 없음,
  DB 비밀번호 없음. SQL 실행과 함수 배포는 사람이 로그인해야 한다.
  그래서 **코드로 할 수 있는 것을 전부 끝내고 절차서를 만들었다** ([[24 라이선스 서버 배포]])
- **가장 위험한 이음매를 배포 전에 검증했다.** 서버(Deno)와 클라이언트(파이썬)의
  서명이 어긋나면 증상은 "모든 고객의 활성화 실패"인데, 서버를 켜기 전에는
  드러나지 않는다.
  - 서명 로직을 `supabase/functions/license/sign.ts` 로 분리 (Deno 전용 API 미사용)
  - `tests/test_edge_signature.py` 가 **실제 sign.ts 를 Node 로 불러** 서명을 만들고
    파이썬 클라이언트로 검증 — 실제 팩 16KB·한글·이모지·제어문자까지 통과
  - 기기 지문/만료/팩을 한 바이트만 바꿔도 검증이 실패하는 것까지 고정
- **설계 변경 1건 — 팩을 환경변수에서 스토리지로.** 프롬프트+시드를 합치면 48KB라
  시크릿에 담기 부담스럽고, 무엇보다 **팩을 갈 때마다 함수를 재배포**해야 했다.
  비공개 버킷 `license-packs/pack.json` 에서 읽고 60초 캐시한다 —
  팩 회전(docs/20 L3)의 전제를 이제야 갖췄다
- 서명 키 생성기에 `--out` 추가. 기본은 **화면에만** 찍는다 — 개인키가 파일로
  남지 않게. (이 세션에서 **개인키를 만들지 않았다**: 대화 기록에 남기지 않고
  사용자가 직접 만드는 편이 안전하다)
- `SERVER_DEFAULT` 를 실제 프로젝트 주소로 교체
- 회귀 9종 통과 (골든 62 · LocalStore 36 · 스모크 26 · 볼트 69 · 휴대폰 37 ·
  업데이트 60 · 라이선스 56 · 스케줄 30 · **Edge 서명 9**)

## 미검증/대기 항목

| 항목 | 조건 |
|---|---|
| Claude 실호출 (조사·토론·구조화 종합) | 크레딧 충전 |
| GPT 전체 | `OPENAI_API_KEY` 등록 |
| 3자 토론 품질 | 위 두 항목 해결 |
| Gamma/Canva/Slides 실생성 | 각 키 등록 |
| 감시 저장·스케줄 실행 e2e | `ra_watches` 테이블 생성 + Actions Secrets |
| 이메일·카카오톡 실발송 | SMTP 앱 비밀번호 / 카카오 리프레시 토큰 |

관련: [[00 홈]] · [[09 향후 고도화 백로그]]
