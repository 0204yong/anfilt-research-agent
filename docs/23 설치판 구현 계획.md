---
tags: [plan, roadmap, installed, testing, design]
updated: 2026-08-09
---

# 23 설치판 구현 계획

> 상태: **0~4단계 완료 (2026-08-09) · 5단계 대기**.
> **임베디드 파이썬 방식 확정 — 설치 파일 73MB, 설치 62초, 첫 기동 3.1초, 제거 시
> 고객 데이터 보존까지 실측 통과.** 상세는 아래 "0단계 실측 결과".
> 구조는 [[22 설치판 아키텍처]]. 이 문서는 **순서와 "끝났다"의 정의**다.

## 원칙 세 가지

1. **체험판을 깨뜨리지 않는다.** 현행 호스팅 앱은 영업 도구다
   (→ [[19 두 제품 구성과 자동 업데이트]]). 모든 단계 끝에 **체험판이 여전히 뜨는지**
   확인한다. Supabase 경로를 지우지 않는다.
2. **각 단계는 독립적으로 가치를 인도한다.** 중간에 멈춰도 그때까지가 쓸모 있어야 한다.
3. **파이프라인 로직에 손대지 않는다.** 프롬프트 팩 분리는 문자열의 출처만 바꾸는
   기계적 치환이다. 출력이 달라지면 그건 버그다 (2단계 DoD에 회귀 비교를 넣는 이유).

---

## 0단계 — 패키징 스파이크 (1주, 선행 필수)

> **여기 결과로 나머지 계획이 확정된다.** 유일하게 실측이 없는 미지수다.

- 목표: **깨끗한 Windows에서 설치 → 실행 → 조사 1건 완료**
- 작업
  - 런타임 동봉 방식 실측: 임베디드 파이썬 vs 포터블 venv
  - Streamlit을 헤드리스로 띄우는 런처, 빈 포트 탐색, 단일 인스턴스
  - `pywebview` 창 vs 기본 브라우저 — 용량·안정성 비교
  - Inno Setup per-user 설치 (`PrivilegesRequired=lowest`), 바로가기, 제거
  - **한글 사용자명 경로**(`C:\Users\홍길동\…`)에서 전 과정 실측
  - 국내 백신(알약·V3) 설치·실행 통과 확인
- **DoD**
  - [ ] 파이썬이 없는 Windows 11 PC에서 설치 후 Gemini 키로 라이트 조사 1건 성공
  - [ ] 한글 경로에서 동일하게 성공
  - [ ] 제거 후 잔여 파일이 데이터 폴더 외에 없음
  - [ ] 설치 용량 500MB 이하, 기동 5초 이내
- **실패 시**: 배포 형태만 바꾼다(포터블 zip + `시작.bat`). **1~7단계 설계는 그대로 유효**하다.

### ✅ 0단계 실측 결과 (2026-08-07)

**결론: 임베디드 파이썬 방식으로 간다.** 배포 형태를 바꿀 이유가 없다.
산출물은 `packaging/` — `build.ps1`(빌드) · `launcher.py`(런처) · `installer.iss`(인스톨러) · `app.ico`.

| 항목 | 목표 | 실측 |
|---|---|---|
| **설치 파일 크기** | — | **73MB** (폴더 340MB → 압축률 21%) |
| 설치 시간 | — | **62초** (선컴파일 포함) |
| 설치 후 디스크 | 500MB 이하 | **377MB** (다이어트 전 533MB) |
| **설치 후 첫 기동 → 화면** | 5초 이내 | **3.1초** |
| 2회차 기동 | — | **1.9초** |
| 빌드 시간 | — | 160초(런타임) + 77초(인스톨러 컴파일) |
| 파일 수 | — | 11,301개 |

**통과한 것**

- 임베디드 3.12.10 런타임에서 **라이트 조사 1건 e2e 성공** (Gemini, 5탭 UI·보고서 생성)
- **시스템 파이썬과 완전 격리** — `sys.path` 3개가 전부 런타임 내부.
  파이썬이 없는 PC에서 동작한다는 가장 직접적인 증거다
- **Supabase 없이 정상 동작** — 키가 없으면 "설정하면 이력이 저장됩니다" 안내만 뜨고
  기능 저하 없이 진행. 1단계 `LocalStore` 전까지도 배포 가능한 상태
- 보고서 3종(PPT·Word·Excel) 생성 — 한글·특수문자 포함, 전부 정상 OOXML
- **한글 경로**(`…\한글 테스트 폴더\리서치에이전트`)에서 기동 2.7초·보고서 생성 정상
- `pages/` 3개 등록 확인 — 사이드바 내비 + 탭 제목 ([[08 작업 이력]] 26번 판별법 통과)
- 런처: 점유 포트 회피(8501→8502) · 단일 인스턴스 감지 · 로그 파일 기록 ·
  앱 모드 브라우저(msedge) 발견

**실측에서 나온 결정·함정 7건**

1. **MAX_PATH(260자)** — 롱패스가 꺼진 기본 상태에서 **빌드가 실패한다**(pip 벤더 경로).
   빌드 작업 폴더를 짧게 유지해야 한다. 실제 설치 위치는
   `%LOCALAPPDATA%\Programs\ANFILT\ResearchAgent`(57자) + 최장 상대경로 142자 = **199자**로
   여유 61자. 사용자명이 매우 길면 좁아지므로 **설치 경로를 더 깊게 만들지 않는다**
2. **`.ps1` 은 UTF-8 BOM으로 저장해야 한다** — Windows PowerShell 5.1은 BOM이 없으면
   ANSI(cp949)로 읽어 **한글 주석이 깨지고 구문 오류가 난다**. 이 프로젝트의 상습 함정
   (→ [[11 원커맨드 배포]])이 빌드 스크립트에서 재발
3. **`google-api-python-client` 98MB를 뺀다** — Google Slides 엔진 전용이고, 그 엔진은
   `ImportError`를 잡아 안내를 내며 키가 없으면 UI에 뜨지도 않는다
   ([gslides_engine.py:31](core/ppt_engines/gslides_engine.py:31)). 전체의 18%
4. `__pycache__` 94MB는 빌드 때 지우지만 **첫 실행에서 되살아난다**. 그래서 첫 기동이
   느리다 — 이 문제의 해법이 아래 8번(설치 시 선컴파일)이다
5. **빌드가 `.streamlit/` 를 통째로 복사하면 로컬 `secrets.toml` 이 고객 PC로 나간다.**
   `config.toml` 만 골라 담고, 산출물에 대해 **비밀 파일·키 패턴 스캔**을 붙여 걸리면
   빌드를 실패시킨다 (→ [[11 원커맨드 배포]] 시크릿 사전 검사와 같은 사상)
6. 그 스캔의 첫 판은 `certifi\cacert.pem`(정상 CA 번들)을 잡았다 → **검사 범위를
   우리가 담은 앱 영역으로 한정**. 런타임의 서드파티 파일까지 보면 오탐이 난다
7. **별도 런처 exe를 컴파일하지 않기로 했다.** 바로가기가 `runtime\pythonw.exe` 로
   `launcher.py` 를 부르고 아이콘만 씌운다 — 컴파일 산출물이 없으면 백신 오탐 여지가
   그만큼 준다 ([[18 설치형 패키지]] 2절과 같은 이유)
8. **설치 후처리로 바이트코드를 선컴파일한다.** 이게 없으면 진짜 첫 실행이 **10.1초**다.
   `compileall -j 0` 이 **15초**를 쓰면 첫 기동이 **3.1초**로 떨어진다. `.pyc` 는 설치
   시점에 만들어지므로 **다운로드 용량에는 영향이 없다**(73MB 그대로).
   → 인스톨러 `[Run]` 1번 항목.
   ⚠️ 측정 함정: 빌드 직후 스모크 테스트를 돌리면 캐시가 이미 덥혀져 "4.3초"처럼
   보인다. **콜드스타트는 반드시 갓 설치한 상태에서 재야 한다.**

**인스톨러 실측 (Inno Setup 6.7.3, 사용자 승인 후 `/CURRENTUSER` 설치)**

- ✅ 무인 설치·제거 종료 코드 0, 시작 메뉴 바로가기 생성
- ✅ **제거 후 프로그램 폴더·바로가기 완전 삭제**
- ✅ **고객 데이터는 남는다** — 볼트 노트 · `.research-agent/agent.db` ·
  `%APPDATA%\ANFILT\config.json` 전부 보존 확인 ([[22 설치판 아키텍처]] 2절 3영역 분리의 목적)
- ✅ 런처 실기동 — 포트 8501 확보, 헬스 ok, **Edge 앱 모드 창**(제목 "멀티 LLM 리서치
  에이전트") 정상, 전용 브라우저 프로필 생성
- ✅ Windows Defender(실시간 보호 켜짐) 검사 **위협 없음·격리 없음**
- ⚠️ **국내 백신(알약·V3)은 이 PC에 없어 미검증** — 고객 배포 전 별도 확인 필요.
  코드 서명 인증서를 사면 이 위험이 크게 준다 ([[20 라이선스와 복제 방지]] L0)
- ⚠️ `dist/` 는 `.gitignore` 에 추가했다 — 73MB 인스톨러가 저장소에 들어가면 안 된다.
  배포는 GitHub Releases (→ [[19 두 제품 구성과 자동 업데이트]] 4.3절)

---

## 1단계 — 저장 계층 분리 (`LocalStore`)

- 목표: Supabase 없이 로컬 폴더 + sqlite로 전 기능이 도는 상태
- 작업
  - `core/appdirs.py` · `core/settings.py` · `core/edition.py`
  - `core/store.py` → `SupabaseStore` 클래스로 정리 (**동작 불변**)
  - `core/store_local.py` — `LocalStore` + `VaultCache` + sqlite 스키마·마이그레이션
  - `record_to_state` → `core/runs.py` 로 이동 (두 구현 공유)
  - `vault_sync.ensure_vault_seeded(store, now)` — 폴더 대상
  - 호출부 주입: app.py 12곳 · pages 6곳 · `watch_runner` · `watch_run.py --vault`
  - `st.cache_data` 캐시 키에 **볼트 경로 포함** (체험판은 trial_code — 17번에서 잡은 유출 경로)
- **DoD**
  - [x] 로컬에서 Supabase 키 **없이** 볼트 축적 → 비서 답변까지 성공
  - [x] 같은 시나리오를 Supabase 키로 실행 = **체험판 회귀 없음** (R1)
  - [x] 원자적 쓰기·mtime 충돌: 외부 편집 후 업서트 → 사용자 편집 보존 + `conflicts/` 생성
  - [x] 스냅샷 캐시 동작 (2회차 3ms)
  - [x] 두 연결 동시 쓰기 시 sqlite 잠금 오류 없음 (WAL)

### ✅ 1단계 실측 결과 (2026-08-07)

**결론: 저장소가 갈렸고 두 경로 모두 산다.** `tests/test_store_local.py` **36항목 전부 통과**.

구조 — 호출부를 거의 건드리지 않은 방법

```python
from core import store as store_mod
store = store_mod.active()      # 이후 store.vault_list() 등은 그대로
```

저장소를 **`store` 라는 이름의 객체로 받는다.** 덕분에 app.py 12곳·pages 6곳의
`store.vault_list()` 호출이 한 줄도 바뀌지 않았다. 동시에 **모듈 수준 저장 함수가
사라져** "어느 볼트인지 정하지 않고 볼트를 부르는" 코드는 이제 작성할 수 없다
(→ [[17 다중 사용자와 테넌트 분리]]가 요구한 성질을 여기서 확보).

| 파일 | 역할 |
|---|---|
| `core/store.py` | 에디션에 따라 저장소를 고르는 **해석기**로 축소 |
| `core/store_supabase.py` | 기존 구현을 `SupabaseStore` 클래스로 이동 (동작 불변) |
| `core/store_local.py` | `LocalStore` + `VaultCache` + sqlite 스키마 |
| `core/runs.py` | `record_to_state` 분리 (두 구현이 공유하는 순수 변환) |
| `core/appdirs.py` · `settings.py` · `edition.py` | 경로·설정·에디션 |

검증

- **정식판**: Supabase 환경변수를 **지운 채** 앱 기동 → 지식 비서 방문만으로
  볼트 폴더에 **시드 35노트 자동 생성**(한글 폴더 `entities/규제·기준/`, 태그·별칭
  프론트매터, `_index/entities.json`) → Obsidian이 바로 열 수 있는 상태
- **체험판(R1)**: 같은 코드로 Supabase 경로 정상 — 사이드바 "🗂 이전 조사"에 실제
  이력 표시, 볼트 37파일 조회, 3개 화면(조사·모니터링·비서) 모두 렌더
- 에디션 판정: `RA_EDITION` > `<설치폴더>/version.json` > 기본 trial.
  **저장소에서 그냥 실행하면 지금까지와 똑같이 동작**한다(회귀 0)
- 정식판인데 볼트 미등록이면 `active()` 가 `None` → 오류가 아니라 **마법사로 보내라는 신호**

이번에 새로 막은 것

- **캐시 키에 볼트를 넣었다** — `_list_runs_cached(store_key)` · `_watches(store_key)` ·
  `_vault(store_key)`. 없으면 볼트를 바꿔도 옛 목록이 캐시에서 나온다
  (→ [[17 다중 사용자와 테넌트 분리]] 3.7절에서 예고한 유출 경로)
- **사용자 편집을 이기지 않는다** — 읽은 시각의 mtime과 다르면 덮어쓰지 않고
  앱 버전을 `.research-agent/conflicts/` 에 보관. 자동 병합은 하지 않는다
- **경로 탈출 차단** — `../` 를 포함한 상대경로는 볼트 밖으로 못 쓴다(zip-slip 방어).
  4단계의 체험판 볼트 가져오기가 이 방어를 그대로 쓴다
- **전체 교체가 조사 이력을 죽이지 않는다** — `vault_replace_all` 이 `.research-agent/`
  와 `.obsidian/` 을 건드리지 않음을 테스트로 고정
- `watch_run.py --vault` 추가 — 작업 스케줄러가 볼트를 지정해 부른다

## 2단계 — 프롬프트 팩 분리

> 라이선스보다 **먼저** 한다. 나중에 하면 전 모듈을 다시 헤집는다
> (→ [[20 라이선스와 복제 방지]]).

- 목표: 프롬프트·스키마·시드가 코드 밖으로 나오고, 앱은 `packs.get()` 으로만 읽는다
- 작업
  - `core/packs.py` — 이 단계에서는 **서명 없는 로컬 JSON 팩**
  - `pipeline.py`·`ontology.py`·`librarian.py`·`watch.py`·`light.py` 의
    **문자열 리터럴과 스키마만** 팩 조회로 치환 (조립 함수는 그대로)
  - `vault_seed/` 35노트를 팩의 `seed` 로
- **DoD**
  - [x] 같은 입력으로 **분리 전/후 프롬프트 문자열이 바이트 단위로 동일** (자동 비교 테스트)
  - [x] 라이트 조사 1건 e2e 정상
  - [x] 팩 파일을 지우면 조사가 **명확한 안내와 함께** 중단 (스택트레이스 노출 금지)

### ✅ 2단계 실측 결과 (2026-08-07)

**결론: 프롬프트가 코드 밖으로 나왔고 문구는 한 글자도 안 바뀌었다.**

방법 — **골든 스냅샷을 먼저 뜨고 분리했다.** `tests/prompt_golden.py` 가 고정 입력으로
프롬프트·스키마·구성표 **62항목**을 만들어 저장하고, 분리 후 바이트 단위로 비교한다.
LLM을 부르지 않아 빠르고 결정적이다. 이 순서가 아니었으면 "비슷해 보이는데 다른"
프롬프트를 못 잡았을 것이다.

| 옮긴 것 | 어디서 |
|---|---|
| 조사·토론·채점·종합 프롬프트, 분량 계획 문구, 채점 기준 9종, 보고서 스키마 | `pipeline.py` |
| 추출 프롬프트·스키마, 엔티티 타입 6종, 술어 12종, 축적 지식 지시문 | `ontology.py` |
| 사서 시스템 프롬프트·답변 프롬프트·스키마 | `librarian.py` |
| 검색·정리·다이제스트 프롬프트, 스키마 2종 | `watch.py` |
| 업무 템플릿 5종, 구조화 프롬프트 | `light.py` |

→ `core/prompts/pack.json` (**prompts 32 · schemas 5 · config 8**),
접근은 `core/packs.py` 의 `get` / `render` / `schema` / `conf` 넷뿐이다.
7단계에서 서명·서버 인출·암호화 캐시로 승격해도 **호출부는 바뀌지 않는다.**

설계 판단 셋

- **치환자는 `string.Template`(`$name`)** — 프롬프트에 JSON 스키마 예시 같은 중괄호가
  섞일 수 있어 `str.format` 은 위험하다. 프롬프트 본문에 `$` 가 없음을 먼저 확인했다.
  `safe_substitute` 가 아니라 `substitute` 를 쓴다 — 못 채운 치환자가 프롬프트에
  `$name` 으로 남는 대신 **즉시 터진다.**
- **임포트 시점에 팩을 읽지 않는다.** 팩이 없어도 앱은 떠야 하고 볼트 열람은
  계속돼야 하기 때문(→ [[22 설치판 아키텍처]] 8절). PEP 562 모듈 `__getattr__` 로
  **접근하는 순간** 꺼낸다 — 프록시가 아니라 진짜 dict/list 라 SDK 에 그대로 넘어간다.
  대신 쓰는 쪽이 `from core.pipeline import REPORT_SCHEMA` 대신
  `from core import pipeline` + `pipeline.REPORT_SCHEMA` 를 써야 한다(호출부 갱신 완료).
- **조립 로직은 코드에 남겼다.** 팩은 문구를, 코드는 "어디에 무엇을 끼울지"를 안다.
  분량 계획도 **개수 산식은 코드, 서술 문구는 팩**으로 갈랐다.

검증

- 골든 비교 **62항목 · 달라짐 0 · 사라짐 0** (pipeline 분리 후 1회, 전체 분리 후 1회)
- **실조사 e2e**: Gemini 라이트 모드로 "EU CBAM 인증서 가격 결정 구조" → 섹션 3·표 1·
  핵심발견 3·출처 2 정상 생성. 팩 프롬프트로 실제 LLM이 동작한다
- **팩 제거 시**: `is_available()` False, 안내 문구 있는 `PackError`,
  **볼트 쓰기·읽기는 그대로 동작** (설계서 22 8절대로)
- UI 가드 `ui_common.pack_required()` — 조사 시작 버튼 비활성·템플릿 선택 숨김,
  볼트 관리는 계속 노출
- `tests/test_store_local.py` 36항목 재통과 (1단계 무회귀)

부수 효과: 프롬프트를 **앱 재배포 없이** 갱신할 수 있게 됐다. ESG 규제가 매년 바뀌는
도메인에서 이건 라이선스와 무관하게도 값이 있다.

## 3단계 — 키·설정 화면

- 목표: 사용자가 화면에서 자기 키를 등록하고 검증한다
- 작업
  - `core/keys.py` — OS 키체인 read/write
  - `providers/*` 생성자에 `api_key` 인자 (3사) · `config.py` 가 키체인에서 조회
  - `pages/0_⚙️_설정.py` — 키 등록·**검증 버튼**(최소 호출 1회)·모델 오버라이드
  - 온보딩 순서: **Gemini 먼저** (무료 티어 → 카드 없이 시작, 라이트 모드로 전 기능 동작)
- **DoD**
  - [x] 키 등록 후 **재시작 없이** 조사 성공
  - [x] 오타 키를 넣으면 검증 버튼이 즉시 잡아냄 (20분 조사 후 실패 금지)
  - [x] `os.environ` 에 사용자 키가 올라가지 않음 (코드 검사 + 실행 중 확인)
  - [x] 키를 지우면 해당 프로바이더만 비활성, 나머지는 정상

### ✅ 3단계 실측 결과 (2026-08-07)

**결론: 키가 화면에서 관리되고, `os.environ` 을 거치지 않는다.**

| 파일 | 역할 |
|---|---|
| `core/keys.py` (신규) | 키체인 read/write/delete. 읽기 순서 = **키체인 → 환경변수** |
| `core/config.py` | `api_key_for(spec)` 신설, `has_key` 가 그것을 씀. 모델 오버라이드가 설정 파일도 본다 |
| `core/providers/*` | 생성자에 `api_key` 인자 (3사) + 팩토리가 주입 |
| `core/providers/__init__.py` | `verify_key(spec, key)` — 최소 호출 1회로 키 검증 |
| `pages/0_⚙️_설정.py` (신규) | 키 등록·검증·삭제, 모델 오버라이드, 프로그램 정보 |

설계 판단

- **키체인 백엔드가 없으면 조용히 환경변수로 떨어진다.** 체험판(리눅스 헤드리스)에는
  백엔드가 없으므로, 같은 코드가 두 에디션에서 다 돌게 하려면 이 폴백이 필수다.
  `keyring.backends.fail.Keyring` 인지 확인해 "있는 척하는 백엔드"를 걸러낸다
- **SDK 가 환경변수를 암묵적으로 읽게 두지 않는다.** `api_key_for()` 로 꺼내
  생성자에 넘긴다 — 그래야 화면에서 등록한 키가 **재시작 없이** 반영되고,
  사용자 키가 프로세스 전역에 올라가지 않는다 (→ [[17 다중 사용자와 테넌트 분리]] 3.5절)
- **화면에 키를 다시 보여주지 않는다.** 등록 여부와 끝 4자리, 그리고 출처
  (이 PC에 저장됨 / 환경변수)만 표시한다
- 온보딩은 **Gemini 먼저** — 무료 티어라 카드 없이 시작할 수 있고, 기본이 라이트
  모드라 키 하나로 전 기능이 돈다. Claude·GPT 는 "토론 품질을 올리고 싶을 때"로 배치

검증 (패키지 런타임에서 — 개발 파이썬에는 keyring 이 없어 폴백만 확인된다)

- 키체인 set/get/delete 왕복 정상, **`os.environ` 오염 없음**
- **키 없이 띄운 앱**이 외부에서 키체인에 등록한 키를 **재시작 없이 인식** →
  설정 화면 "✅ 등록됨 · …3z6Q · 이 PC에 저장됨", 조사 버튼 활성화
- 잘못된 키 검증: `400 INVALID_ARGUMENT — API key not valid` 즉시 반환.
  빈 키는 호출 없이 "키가 없습니다"
- Claude 키를 넣었다 지워도 Gemini 는 그대로 동작 (프로바이더별 독립)

**실측에서 잡은 버그 1건 — `version.json` BOM**

PowerShell 의 `Out-File -Encoding utf8` 은 **BOM을 붙인다.** 그래서
`json.loads(read_text(encoding="utf-8"))` 가 조용히 실패해 **버전이 늘 `dev`** 로
나왔다. 설정 화면에 "버전 dev"가 뜬 덕에 발견했다.

이건 표시만의 문제가 아니다 — **6단계 자동 업데이트는 이 버전으로 매니페스트와
비교**하므로, 그대로 뒀으면 업데이트가 영영 안 걸리거나 매번 걸렸을 것이다.
읽는 쪽을 `utf-8-sig` 로 고치고(양쪽 다 받는다), 굽는 쪽(`build.ps1`)도 BOM 없이
쓰도록 바꿨다. `runtime_version()` 도 함께 노출했다(델타/전체 판정용).

**2단계에서 흘린 버그 1건 — `ontology._norm` 유실**

프롬프트 팩 분리 때 리터럴 블록을 잘라내다 `_norm` 헬퍼가 **같이 지워졌다.**
골든 테스트는 프롬프트만 비교하므로 못 잡았고, 실조사에서
`축적 지식 주입 실패 — name '_norm' is not defined` 로 드러났다.
안전장치(주입 실패는 삼키고 계속)가 삼켜서 **조사는 성공한 채로 지식만 조용히
주입되지 않는** 상태였다 — 가장 나쁜 종류의 조용한 고장이다.

- 2단계 이전 커밋과 **최상위 정의 목록을 기계적으로 비교**해 유실 범위를 확정했다
  (나머지 사라진 이름들은 전부 팩으로 옮긴 의도된 제거)
- 재발 방지로 `tests/test_core_smoke.py` **26항목** 신설 — 프롬프트가 아닌
  **LLM 없는 순수 로직**(온톨로지 병합·인덱스·주입 매칭·검토 필요 반영,
  사서 검색, 감시 지문·URL 정규화, 볼트 zip 왕복)을 전부 한 번씩 실행한다.
  골든 테스트의 사각지대가 정확히 여기였다

> 남은 사소한 항목: 체험판에서도 사이드바에 "⚙️ 설정" 항목이 보인다(열면
> "설치판 전용" 안내). Streamlit 이 `pages/` 를 자동 등록하기 때문이며,
> 동적 내비게이션 전환은 5단계에서 다룬다.

## 4단계 — 최초 실행 마법사 · 볼트 자동 생성

- 목표: **"설치하면 볼트가 자동으로 생긴다"**
- 작업
  - 폴더 선택(기본 `~\Documents\리서치에이전트 지식볼트`) → 생성 → 시드 35노트
  - `.obsidian/` 프리셋(그래프 뷰·태그 패널) 동봉
  - `obsidian://open?path=…` 로 열기 · 미설치 시 다운로드 안내
  - **체험판 볼트 zip 가져오기** 단계 (`parse_vault_zip` 재사용 → 폴더 전개 + sqlite 적재 + 인덱스 재생성)
  - 볼트 여러 개 등록·전환 (고객사별 분리)
- **DoD**
  - [x] 새 PC에서 마법사만 따라가면 Obsidian에 볼트가 열리고 태그 패널이 보임
  - [x] 체험판에서 받은 zip을 가져오면 노트·조사 이력이 그대로 복원
  - [x] 볼트 전환 시 조사 이력·감시가 함께 바뀜
  - [x] 기존 Obsidian 볼트의 하위 폴더로 설치하는 경로도 동작

### ✅ 4단계 실측 결과 (2026-08-09)

**결론: 폴더를 고르는 순간부터 Obsidian 이 열리기까지가 한 화면으로 이어진다.**
`tests/test_vault_setup.py` **69항목 전부 통과**.

| 파일 | 역할 |
|---|---|
| `core/vault_setup.py` (신규) | 진단 · 만들기 · `.obsidian/` 프리셋 · Obsidian 등록 · 폴더 선택 · zip 가져오기 |
| `ui_vault.py` (신규) | `wizard()`(첫 화면) · `manager()`(설정 페이지) — 조각을 공유한다 |
| `app.py` | 볼트가 없으면 **첫 화면을 마법사로 대체** |
| `pages/0_⚙️_설정.py` | 맨 위에 지식볼트 절 (추가·전환·제거·가져오기) |

설계 판단 넷

- **키보다 볼트를 먼저 묻는다.** 볼트는 이 제품이 무엇인지 설명해 주지만,
  API 키는 설명 없이 요구하면 벽으로 느껴진다. 팩 검사보다도 앞이다 —
  볼트를 만드는 데는 프롬프트가 필요 없다
- **남의 Obsidian 설정을 덮어쓰지 않는다.** `.obsidian/` 이 이미 있으면 프리셋을
  건너뛴다. 사용자가 쓰던 플러그인·테마·작업공간을 날리는 건 되돌릴 수 없다
- **run.json 은 폴더에 쓰지 않는다.** 정식판의 조사 아카이브는 sqlite 하나다.
  같은 기록을 폴더에도 두면 둘이 어긋날 수 있고, 어긋났을 때 어느 쪽이
  진짜인지 답할 방법이 없다
- **폴더 선택 창은 PowerShell 로 띄운다.** 임베디드 파이썬에는 tkinter 가 없다.
  명령을 **Base64(UTF-16LE)** 로 넘겨 한글·인용부호가 콘솔 코드페이지를 안 타게
  했다 (→ 이 프로젝트의 상습 함정, [[11 원커맨드 배포]])

**실측으로 잡은 것 3건 — 전부 화면에서 눌러 봐야 나오는 것들이었다**

1. **`obsidian://open?path=` 는 Obsidian 이 *아는* 볼트에만 통한다.** 갓 만든
   폴더에 그 링크를 쓰면 Obsidian 이 **오류 창**을 띄운다 (1.13.4 실측).
   → `%APPDATA%\obsidian\obsidian.json`(볼트 목록)에 **더하기만** 하는
   `register_with_obsidian()` 을 만들었다. 남의 앱 설정 파일이라 규칙을 셋 뒀다 —
   파싱 실패면 아무것도 쓰지 않고, 원자적으로 쓰고, 실패하면 조용히 False 를
   내고 화면이 "Open folder as vault" 안내로 떨어진다.
   등록 후 실제로 **볼트가 열리고 오른쪽에 태그 패널이 펼쳐진 채** 뜨는 것,
   Obsidian 이 우리 `workspace.json` 을 읽어 자기 형식으로 다시 쓰면서도
   **태그 탭을 첫 자리에 유지**하는 것까지 확인했다
2. **합치기 가져오기가 색인을 통째로 갈아 없앴다.** zip 안의 노트만으로
   `_index/entities.json` 을 다시 만들어서, 원래 있던 시드 35개가 색인에서
   사라졌다(색인 엔티티 2개). 노트 파일은 멀쩡한데 **비서·주입이 조용히
   아무것도 못 찾는** 상태 — 3단계 `_norm` 유실과 같은 종류의 고장이다.
   → 색인은 **합쳐진 볼트 전체를 다시 읽어** 만든다. 회귀 테스트로 고정
3. **볼트를 만든 순간 마법사가 사라졌다.** 게이트가 `active() is None` 만 보니
   생성 직후 조건이 풀려 "Obsidian 으로 열기" 화면이 통째로 없어졌다 —
   마법사의 마지막 한 걸음이 사라지는 셈. 세션 상태를 조건에 넣어,
   사용자가 "조사 시작하기"를 누를 때까지 마법사가 화면을 지킨다

부수로 고친 것

- **팩이 없어도 볼트는 만들어진다.** 시드 노트를 읽으려면 프롬프트 팩이 필요한데
  (관계 술어 어휘), 팩이 없다고 볼트조차 못 만들면 사용자가 자기 데이터를 둘 곳을
  잃는다 — [[22 설치판 아키텍처]] 8절이 금지하는 상태다. 폴더·프리셋은 항상
  만들고, 시드 실패는 삼키지 않고 화면에 따로 알린다
- `pages/1_📡_모니터링.py` 의 `pack_required()` 중복 호출 제거 (2단계 잔재 —
  팩이 없을 때 같은 오류가 두 번 떴다)
- `edition.py` 의 `vault_zip_import` → `vault_import_wizard` 로 이름 정정.
  체험판 사이드바의 zip 업로드(→ [[13 지식 볼트와 온톨로지]])와 헷갈릴 이름이었다

## 5단계 — 런처·인스톨러·휴대폰 접속

- 목표: 남에게 건넬 수 있는 물건
- 작업
  - 0단계 결과를 제품 품질로: 런처·아이콘·바로가기·제거
  - **휴대폰 접속 토글** — `0.0.0.0` 재기동 + QR + **PIN 게이트 강제**
    (`ui_common.require_password` 재사용, PIN 미설정이면 안 켜짐 — fail-closed)
  - 비서 페이지 모바일 레이아웃 (→ [[21 휴대폰에서 쓰기]])
  - 진단 화면 — 버전·경로·키 상태·최근 오류 (**키·볼트 내용 제외**한 내보내기)
- **DoD**
  - [ ] 같은 Wi-Fi 폰 브라우저에서 QR → PIN → 비서 답변까지
  - [ ] 휴대폰 접속 OFF 상태에서 외부 접속이 실제로 거부됨
  - [ ] PIN 미설정 시 토글이 켜지지 않음

## 6단계 — 자동 업데이트

- 목표: 패치가 고객 PC까지 간다 (→ [[19 두 제품 구성과 자동 업데이트]] 4절)
- 작업
  - 홈페이지에 `releases/latest.json` · GitHub Releases 업로드 잡
  - 클라이언트: 확인 → 델타/전체 판정(`runtime_version`) → sha256 검증
  - `updater.exe`: 앱 종료 → `app\`→`app.bak\` → 교체 → 재시작 → 자가진단 → `app.bak\` 삭제
  - 실패 시 롤백. **조사 실행 중에는 재시작하지 않는다**
- **DoD**
  - [ ] 구버전 설치 → 릴리스 → **자동으로 신버전이 됨** (신규 설치만 테스트하면 이 경로가 안 잡힌다)
  - [ ] 다운로드 중 강제 종료 후 재시작해도 앱이 멀쩡함
  - [ ] sha256 불일치 파일은 적용되지 않음
  - [ ] 볼트·설정·라이선스가 업데이트 후 그대로

## 7단계 — 라이선스 (판매 가능 지점)

- 목표: 구매자만 실행 가능 (→ [[20 라이선스와 복제 방지]])
- 작업
  - `supabase-ra-license-setup.sql` — `ra_licenses` · `ra_activations`
  - Supabase Edge Function — `/activate` · `/refresh` · `/deactivate`
  - `core/licensing.py` — 기기 지문·Ed25519 검증·AES-GCM 캐시·유예 처리
  - 팩을 **서명·인출 방식으로 승격** (2단계의 로컬 팩을 대체)
  - `/admin/` 에 발급·정지·**기기 해제** 화면 (해제가 없으면 지원 문의 1위가 된다)
  - 설정 화면에 라이선스 상태·기기 목록
- **DoD**
  - [ ] 미활성 상태에서 조사·비서가 잠기고 **볼트 열람·내보내기는 열림** (22번 8절 표대로)
  - [ ] 설치 폴더를 통째로 다른 PC에 복사 → **동작하지 않음**
  - [ ] 좌석 초과 활성화가 거부됨 · 기기 해제 후 재활성화 성공
  - [ ] 네트워크를 끊어도 유예 기간 내 정상 동작
  - [ ] **활성화 서버를 내려도** 앱이 유예 처리로 계속 동작

## 8단계 — 모니터링 로컬 스케줄

- 작업: 작업 스케줄러 등록(설치 옵션), `is_due` **따라잡기**
  ([watch.py:115](core/watch.py:115) — 지금은 `now.hour` 정확 일치라 PC가 꺼져 있던 시각은 영영 건너뛴다)
- **DoD**
  - [ ] 08시 감시 등록 후 10시에 PC를 켜면 **그날 안에 실행됨**
  - [ ] 같은 시각대 중복 실행 없음
  - [ ] 앱이 떠 있어도 스케줄러 실행과 sqlite 충돌 없음

---

## 상시 회귀 방어

매 단계 종료 시 아래를 돌린다. 하나라도 깨지면 다음 단계로 가지 않는다.

| # | 확인 | 이유 |
|---|---|---|
| R1 | 체험판(호스팅)이 정상 기동·조사 1건 | 영업 도구가 죽으면 안 된다 |
| R2 | 라이트 조사 e2e (Gemini) | 기본 모드 |
| R3 | 볼트 축적 — 신규/갱신 노트 생성, 인덱스 재생성 | 제품의 핵심 가치 |
| R4 | 비서 답변에 근거 노트 인용 | [[16 지식 비서]] |
| R5 | 볼트 zip 내보내기/가져오기 왕복 | 이주 경로 |
| R6 | 한글 경로·한글 파일명 | 이 프로젝트의 상습 함정 |

## 위험과 대응

| 위험 | 대응 |
|---|---|
| **0단계에서 막힘** | 포터블 zip + `시작.bat` 으로 대체. 1~7단계 설계는 무영향 |
| **팩 분리 중 출력이 미묘하게 바뀜** | 2단계 DoD의 **바이트 단위 프롬프트 비교**로 잡는다 |
| **두 에디션이 갈라짐** | 경계 셋(`store`·`keys`·`packs`)을 넘는 분기는 리뷰에서 반려 ([[22 설치판 아키텍처]] 4절) |
| **백신 오탐** | onefile 회피 + 서명 인증서(매출 후 우선 투자) |
| **업데이트가 조용히 고장** | 화면에 버전·마지막 확인 시각 상시 표시, 실패는 로그가 아니라 **배너** |
| **작업량 과소평가** | 1·2단계가 전체의 절반이다. 3~5단계는 화면 작업이라 상대적으로 빠르다 |

## 판매 가능 최소선

**0~7단계**가 끝나면 판다. 8단계(모니터링 로컬 스케줄)는 없어도 팔 수 있다 —
알림은 이미 이메일·카톡으로 가고, 조사·비서·볼트가 제품의 본체이기 때문이다.

## 다음 행동

**0단계 스파이크부터 시작한다.** 그 결과 없이 1단계 코드를 쓰면, 배포 형태가
바뀔 때 런처·경로 가정이 통째로 흔들릴 수 있다.

관련: [[22 설치판 아키텍처]] · [[19 두 제품 구성과 자동 업데이트]] ·
[[20 라이선스와 복제 방지]] · [[21 휴대폰에서 쓰기]] · [[08 작업 이력]]
