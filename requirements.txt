streamlit>=1.40.0
anthropic>=0.49.0
openai>=1.60.0
google-genai>=1.0.0
python-dotenv>=1.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
python-pptx>=1.0.0
pypdf>=4.0.0
google-api-python-client>=2.100.0
google-auth>=2.23.0
python-docx>=1.1.0
openpyxl>=3.1.0
# 설치판에서 API 키를 OS 자격 증명 저장소에 보관 (→ core/keys.py).
# 백엔드가 없는 환경(호스팅 리눅스)에서는 조용히 건너뛰고 환경변수로 동작한다.
keyring>=25.0.0
# 휴대폰 접속 주소를 QR 로 보여 준다 (→ core/mobile.py). 순수 파이썬이라 가볍고,
# 없으면 QR 없이 주소만 표시하므로 기능이 죽지는 않는다.
qrcode>=7.4.0
